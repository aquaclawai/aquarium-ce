---
phase: 23
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/package-lock.json
  - apps/web/src/index.css
  - apps/web/src/components/ui/sonner.tsx
  - apps/web/scripts/check-i18n-parity.mjs
  - .github/workflows/ci.yml
  - apps/web/src/components/issues/.gitkeep
  - tests/e2e/issues-board.spec.ts
  - packages/shared/src/types.ts
  - .planning/phases/23-issue-board-ui-kanban/23-00-A1-VERIFIED.md
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
must_haves:
  truths:
    - "Five new npm deps installed at pinned versions in apps/web/package.json"
    - "Z-index ladder CSS variables exist in apps/web/src/index.css"
    - "Sonner Toaster overrides zIndex to var(--z-toast) so drag overlay can sit above toasts-in-dropdowns but below critical alerts"
    - "i18n parity script exists and fails when a locale is missing a key"
    - "CI runs i18n parity on every push"
    - "WS broadcast semantics (A1) documented so plan 23-01 can call the correct subscribe method"
    - "packages/shared/src/types.ts WsEventType union includes 'issue:created', 'issue:updated', 'issue:deleted', 'issue:reordered'"
    - "Playwright spec file tests/e2e/issues-board.spec.ts exists with scenario stubs"
    - "apps/web/src/components/issues/ directory scaffolded"
  artifacts:
    - path: apps/web/package.json
      provides: "pinned DnD + virtualization + accessibility deps"
      contains: '"@dnd-kit/core"'
    - path: apps/web/src/index.css
      provides: "z-index ladder CSS variables"
      contains: "--z-drag-overlay"
    - path: apps/web/scripts/check-i18n-parity.mjs
      provides: "i18n parity guard"
      min_lines: 40
    - path: .github/workflows/ci.yml
      provides: "CI step running check-i18n-parity"
      contains: "check-i18n-parity"
    - path: tests/e2e/issues-board.spec.ts
      provides: "Playwright spec for Phase 23 scenarios"
      contains: "'renders columns'"
    - path: packages/shared/src/types.ts
      provides: "issue:* WS event types in the shared union"
      contains: "'issue:created'"
    - path: .planning/phases/23-issue-board-ui-kanban/23-00-A1-VERIFIED.md
      provides: "Wave 0 WS semantics verification note for downstream plans"
      contains: "broadcast(instanceId"
  key_links:
    - from: .github/workflows/ci.yml
      to: apps/web/scripts/check-i18n-parity.mjs
      via: "CI step invokes node script"
      pattern: "check-i18n-parity"
    - from: apps/web/src/components/ui/sonner.tsx
      to: apps/web/src/index.css
      via: "Toaster style reads var(--z-toast)"
      pattern: "--z-toast"
---

<objective>
Foundation wave. Install 5 npm dependencies pinned by 23-RESEARCH §Standard Stack,
establish the z-index CSS ladder per 23-UI-SPEC §Z-Index Ladder, ship the i18n-parity
script + CI wiring per UX5 mitigation, extend the shared WsEventType union so downstream
plans can consume `issue:*` events without rebuilding types, scaffold the component
directory, stub the Playwright spec, and DOCUMENT the A1 WebSocket subscribe semantics
finding so plan 23-01 wires WS correctly. Also: bump `apps/web/src/components/ui/sonner.tsx`
so the Toaster respects the new z-index ladder.

No feature UI here. This plan exists to make plans 01–05 trivially mergeable.

Purpose:
- Unblock Wave 1: board cannot render until deps are installed and WsEventType covers issue:*
- Mitigate UX3 (z-index wars) + UX5 (i18n drift) at the source
- Answer Assumption A1 (WS subscribe semantics) before Wave 1 writes the subscribe call

Output:
- apps/web/package.json delta (5 new deps)
- apps/web/src/index.css z-index ladder
- apps/web/src/components/ui/sonner.tsx toastOptions.style with --z-toast
- apps/web/scripts/check-i18n-parity.mjs
- .github/workflows/ci.yml i18n-parity step
- packages/shared/src/types.ts WsEventType extension
- apps/web/src/components/issues/ directory
- tests/e2e/issues-board.spec.ts scaffold (scenarios skip() until wired)
- .planning/phases/23-issue-board-ui-kanban/23-00-A1-VERIFIED.md (inline note)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md
@.planning/phases/23-issue-board-ui-kanban/23-RESEARCH.md
@.planning/phases/23-issue-board-ui-kanban/23-UI-SPEC.md
@.planning/phases/23-issue-board-ui-kanban/23-VALIDATION.md
@CLAUDE.md
@apps/web/package.json
@apps/web/src/index.css
@apps/web/src/components/ui/sonner.tsx
@packages/shared/src/types.ts
@apps/server/src/ws/index.ts
@apps/server/src/routes/issues.ts
@.github/workflows/ci.yml

<interfaces>
<!-- Observed in the codebase during planning (2026-04-17). Executor should use these verbatim. -->

From apps/server/src/ws/index.ts (line 115-121):
```ts
export function broadcast(instanceId: string, message: unknown): void {
  for (const client of clients) {
    if (client.instanceSubscriptions.has(instanceId)) {
      sendToClient(client.ws, message);
    }
  }
}
```
**A1 finding (matches §Open Question #1 in 23-RESEARCH.md):** The server fans out ONLY to clients whose `instanceSubscriptions` Set contains the key. Phase 17 broadcasts with the workspace id ('AQ') as the key. Therefore the board must call `subscribe('AQ')` on mount — reusing the existing `subscribe(instanceId)` method in WebSocketContext with the workspace id string. NO new `subscribeWorkspace` method is required. Plan 23-01 wires this.

From packages/shared/src/types.ts (line 299-320):
```ts
export type WsEventType =
  | 'instance:status'
  | 'instance:logs'
  | 'instance:event'
  | 'instance:ready'
  | 'instance:gateway_event'
  | 'instance:exec_approval_request'
  | 'instance:exec_approval_resolved'
  | 'group_chat:message'
  | 'group_chat:delivery_status'
  | 'group_chat:typing'
  | 'group_chat:error'
  | 'instance:snapshot_restored'
  | 'notification'
  | 'security_event';
export interface WsMessage {
  type: WsEventType;
  instanceId?: string;
  groupChatId?: string;
  payload: Record<string, unknown>;
}
```
**Gap:** `'issue:created' | 'issue:updated' | 'issue:deleted' | 'issue:reordered' | 'task:cancelled'` are broadcast by the server (verified in routes/issues.ts lines 98, 128-144, 169, 195) but NOT in WsEventType. Board can't `addHandler('issue:reordered', ...)` until this union includes them.

From routes/issues.ts (lines 98-102, 128-144, 169-173, 195-199) — broadcast payload shapes:
```
broadcast('AQ', { type: 'issue:created',   issueId, payload: issue })
broadcast('AQ', { type: 'issue:updated',   issueId, payload: issue })
broadcast('AQ', { type: 'issue:deleted',   issueId })
broadcast('AQ', { type: 'issue:reordered', issueId, payload: { position } })
broadcast('AQ', { type: 'task:cancelled',  taskId, issueId, payload: { taskId, issueId } })
```
The payload field is optional for 'issue:deleted' (no payload), so the interface already permits `payload: Record<string, unknown>` with the field absent — TypeScript will tolerate via `payload?:`. Executor MUST make `payload?: Record<string, unknown>` to match reality.

From apps/web/src/components/ui/sonner.tsx (current toastOptions.style):
Already sets background/border/color/fontSize/borderRadius/boxShadow/padding/lineHeight.
Executor ADDS `zIndex: 'var(--z-toast)'` to this existing object — DOES NOT replace other fields.

From .github/workflows/ci.yml (current jobs.check.steps, lines 15-22):
- npm ci
- npm run build -w @aquarium/shared
- Typecheck: npm run typecheck -w @aquaclawai/aquarium
- Lint: npm run lint -w @aquarium/web
Executor ADDS a new step AFTER Lint: `npm run check:i18n -w @aquarium/web` (needs a package.json script too). Do NOT modify existing steps.

From apps/web/src/i18n/locales/en.json (verified at line 1341): `sidebar` namespace exists with keys `chat, groupChats, dashboard, skills, assistants, ...`. Add `sidebar.issues` key here (plan 23-01 actually consumes it; Wave 0 reserves the keyspace only via script dry-run).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps + extend shared WsEventType + scaffold component dir + create Playwright spec stub</name>
  <files>
    - apps/web/package.json
    - apps/web/package-lock.json
    - packages/shared/src/types.ts
    - apps/web/src/components/issues/.gitkeep
    - tests/e2e/issues-board.spec.ts
    - .planning/phases/23-issue-board-ui-kanban/23-00-A1-VERIFIED.md
  </files>
  <read_first>
    - apps/web/package.json (current deps)
    - packages/shared/src/types.ts (lines 299-320 WsEventType + WsMessage)
    - apps/server/src/ws/index.ts (lines 109-146, broadcast implementations)
    - apps/server/src/routes/issues.ts (lines 98-207, verify broadcast payload shapes)
    - .planning/phases/23-issue-board-ui-kanban/23-RESEARCH.md §Standard Stack (pinned versions)
    - .planning/phases/23-issue-board-ui-kanban/23-UI-SPEC.md §Component Inventory
    - tests/e2e/issues-agents-comments.spec.ts (lines 1-80 — Playwright pattern to clone)
    - .planning/phases/23-issue-board-ui-kanban/23-VALIDATION.md §Per-Task Verification Map
  </read_first>
  <action>
    Step 1 — Install pinned deps into the `@aquarium/web` workspace (exact versions per 23-RESEARCH §Standard Stack, verified 2026-04-17):
    ```bash
    npm install -w @aquarium/web @dnd-kit/core@6.3.1 @dnd-kit/sortable@10.0.0 @dnd-kit/utilities@3.2.2 @tanstack/react-virtual@3.13.24
    ```
    Then install @dnd-kit/accessibility (version unpinned in research, verify latest compatible):
    ```bash
    npm view @dnd-kit/accessibility version     # record version found
    npm install -w @aquarium/web @dnd-kit/accessibility@<version-found>
    ```
    All 5 packages MUST appear in `apps/web/package.json` `dependencies` (not devDependencies).

    Step 2 — Extend WsEventType in `packages/shared/src/types.ts`. In the union at line ~299, ADD these literals AFTER `'security_event'`:
    ```
      | 'issue:created'
      | 'issue:updated'
      | 'issue:deleted'
      | 'issue:reordered'
      | 'task:cancelled'
    ```
    Also change `WsMessage.payload` from `payload: Record<string, unknown>` to `payload?: Record<string, unknown>` (line ~319) because `issue:deleted` broadcasts with no payload. Add a top-level optional `issueId?: string` and `taskId?: string` to WsMessage (lines ~315-320) so board handlers can read them without casts. NO other fields change.

    Step 3 — Create `apps/web/src/components/issues/.gitkeep` (empty file, content: single newline). This reserves the directory so plans 01/02/03/04 can Write into it.

    Step 4 — Create `tests/e2e/issues-board.spec.ts` with the EXACT Playwright scaffold pattern from `tests/e2e/issues-agents-comments.spec.ts` (imports, `API`, `DB_PATH`, `readDb`, `writeDb`, `signUpTestUser`, `uniqueEmail`, `uniqueName` helpers). Then add 7 placeholder scenarios — each must use `test.skip()` body for now so CI passes, with this exact title set (downstream plans remove `.skip` as they wire each scenario):
    ```ts
    test('renders columns', ({}) => test.skip(true, 'wired in 23-01'));
    test('mouse drag', ({}) => test.skip(true, 'wired in 23-02'));
    test('concurrent reorder', ({}) => test.skip(true, 'wired in 23-02'));
    test('own echo', ({}) => test.skip(true, 'wired in 23-02'));
    test('virtualization', ({}) => test.skip(true, 'wired in 23-03'));
    test('virtualization drag', ({}) => test.skip(true, 'wired in 23-03'));
    test('keyboard drag', ({}) => test.skip(true, 'wired in 23-04'));
    test('a11y announcer', ({}) => test.skip(true, 'wired in 23-04'));
    ```
    Use `test.describe.serial('Phase 23 — Issue Board UI (Kanban)', () => { ... })` wrapping the scenarios. Do NOT add any DB fixture yet — plans 01/02/03/04 add their fixtures when wiring.

    Step 5 — Create `.planning/phases/23-issue-board-ui-kanban/23-00-A1-VERIFIED.md` (content below, verbatim):
    ```markdown
    # A1 Verification — WS subscribe semantics (Phase 23 Wave 0)

    **Verified:** 2026-04-17
    **Source:** apps/server/src/ws/index.ts lines 115-121

    The server's `broadcast(instanceId, message)` function filters on
    `client.instanceSubscriptions.has(instanceId)`. Phase 17 broadcasts issue:*
    events with the workspace id 'AQ' as the instanceId parameter.

    **Implication:** The existing `subscribe(instanceId)` method on
    WebSocketContext can be called with the workspace id 'AQ' — NO new
    `subscribeWorkspace` method is needed. `issue:created | issue:updated |
    issue:deleted | issue:reordered | task:cancelled` events will reach any
    client that calls `subscribe('AQ')` on mount.

    **Downstream action (Plan 23-01):** On IssuesBoardPage mount, call
    `subscribe('AQ')`; on unmount, call `unsubscribe('AQ')`. Keep 'AQ' as an
    inline string literal matching the server's `DEFAULT_WORKSPACE_ID` (routes/issues.ts line 23).
    ```

    Constraints:
    - DO NOT modify `WsMessage` beyond making `payload?` optional and adding `issueId?` + `taskId?` (no breaking changes to existing consumers).
    - DO NOT run `npm run build -w @aquarium/shared` in this task (plan 23-01 rebuilds as part of its typecheck step).
    - DO NOT add any runtime logic; this task is pure scaffolding.
  </action>
  <verify>
    <automated>
      grep -q '"@dnd-kit/core": "\^6.3.1"' apps/web/package.json &&
      grep -q '"@dnd-kit/sortable": "\^10.0.0"' apps/web/package.json &&
      grep -q '"@dnd-kit/utilities": "\^3.2.2"' apps/web/package.json &&
      grep -q '"@dnd-kit/accessibility":' apps/web/package.json &&
      grep -q '"@tanstack/react-virtual": "\^3.13.24"' apps/web/package.json &&
      grep -q "'issue:created'" packages/shared/src/types.ts &&
      grep -q "'issue:reordered'" packages/shared/src/types.ts &&
      grep -q "'task:cancelled'" packages/shared/src/types.ts &&
      test -d apps/web/src/components/issues &&
      test -f tests/e2e/issues-board.spec.ts &&
      grep -q "renders columns" tests/e2e/issues-board.spec.ts &&
      grep -q "concurrent reorder" tests/e2e/issues-board.spec.ts &&
      grep -q "virtualization" tests/e2e/issues-board.spec.ts &&
      grep -q "keyboard drag" tests/e2e/issues-board.spec.ts &&
      test -f .planning/phases/23-issue-board-ui-kanban/23-00-A1-VERIFIED.md
    </automated>
  </verify>
  <acceptance_criteria>
    - apps/web/package.json dependencies contain exact versions: @dnd-kit/core ^6.3.1, @dnd-kit/sortable ^10.0.0, @dnd-kit/utilities ^3.2.2, @tanstack/react-virtual ^3.13.24, @dnd-kit/accessibility (any satisfactory version)
    - apps/web/package-lock.json updated (touched in same npm install call)
    - packages/shared/src/types.ts WsEventType union contains 5 new literals: 'issue:created', 'issue:updated', 'issue:deleted', 'issue:reordered', 'task:cancelled'
    - packages/shared/src/types.ts WsMessage.payload field is optional (`payload?:`) and has optional `issueId?: string` + `taskId?: string`
    - apps/web/src/components/issues/.gitkeep exists
    - tests/e2e/issues-board.spec.ts exists with the 7 named skip-scenarios (grep each title)
    - .planning/phases/23-issue-board-ui-kanban/23-00-A1-VERIFIED.md exists and references "broadcast(instanceId" and "subscribe('AQ')"
  </acceptance_criteria>
  <done>
    All 5 deps installed at pinned versions, shared types extended without breaking existing consumers, Playwright scaffold committed, A1 finding documented in a file plan 23-01 reads verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add z-index CSS ladder + migrate Toaster + i18n parity script + CI wiring</name>
  <files>
    - apps/web/src/index.css
    - apps/web/src/components/ui/sonner.tsx
    - apps/web/scripts/check-i18n-parity.mjs
    - apps/web/package.json
    - .github/workflows/ci.yml
  </files>
  <read_first>
    - apps/web/src/index.css (lines 1-120 and line 560-620, verify existing :root block and .visually-hidden and :focus-visible positions)
    - apps/web/src/components/ui/sonner.tsx (current toastOptions.style — MUST preserve all existing keys)
    - apps/web/src/i18n/index.ts (confirms 6 locales loaded: en, zh, fr, de, es, it)
    - apps/web/src/i18n/locales/en.json (line 1341 — current 'sidebar' namespace)
    - .github/workflows/ci.yml (current jobs.check structure)
    - .planning/phases/23-issue-board-ui-kanban/23-UI-SPEC.md §Z-Index Ladder (exact variable names + values)
    - .planning/phases/23-issue-board-ui-kanban/23-RESEARCH.md §Pitfall 3 + §Pitfall 5
  </read_first>
  <action>
    Step 1 — Add z-index ladder to `apps/web/src/index.css`. Locate the `:root {` block (the FIRST `:root` block, around line 46 in the existing file — the one with `--p-brand`, `--p-black` etc.). Append these lines INSIDE that existing `:root { ... }` block, AFTER the existing `--space-10` declaration (preserve the closing `}`). EXACT text:
    ```css
      /* Z-Index Ladder (Phase 23 UX3 — see 23-UI-SPEC §Z-Index Ladder) */
      --z-base: 0;
      --z-dropdown: 10;
      --z-sticky: 20;
      --z-header: 50;
      --z-sidebar: 100;
      --z-sheet: 500;
      --z-modal: 1000;
      --z-drag-overlay: 5000;
      --z-toast: 7000;
      --z-critical-alert: 10000;
    ```
    Do NOT duplicate these in any `:root.dark` / `.dark` block (z-index values are theme-invariant, per 23-UI-SPEC §Z-Index Ladder).

    Step 2 — Edit `apps/web/src/components/ui/sonner.tsx`. Inside the existing `toastOptions.style` object literal, ADD ONE new key: `zIndex: 'var(--z-toast)'`. Preserve every other key (background, border, color, fontSize, borderRadius, boxShadow, padding, lineHeight). Do NOT change the `theme` or `position` props. Do NOT move any existing code.

    Step 3 — Create `apps/web/scripts/check-i18n-parity.mjs` (new directory, new file). ESM script, shebang `#!/usr/bin/env node`. Behaviour:
    - Glob `apps/web/src/**/*.{ts,tsx}` files from the repo root.
    - Extract every `t('key')` and `t("key")` call via regex `/t\(\s*['"]([a-zA-Z0-9_.]+)['"]/g`. Also accept keys used via `i18nKey="..."` attribute via `/i18nKey=['"]([a-zA-Z0-9_.]+)['"]/g`. Deduplicate.
    - Exclude keys that contain `{{` (interpolated — those are user-entered).
    - Load each locale JSON: `apps/web/src/i18n/locales/{en,zh,fr,de,es,it}.json`.
    - For each key (dot-path), check `get(locale, key)` returns a string. If ANY locale is missing ANY key, print `MISSING <locale>: <key>` and exit 1. If all present, print `OK: <N> keys checked across 6 locales` and exit 0.
    - Also check INVERSE: any key present in `en` but missing from any other locale → MISSING (flags scenarios where a developer updated en.json in-IDE but forgot the others).
    - Use only Node built-ins: `node:fs`, `node:path`, `node:url`, `node:process`. No extra deps. Use `fs.globSync` (Node 22+ supports this — CLAUDE.md confirms `Node.js 22+`).
    - Target length ~80-120 lines.

    Step 4 — Add a `check:i18n` npm script to `apps/web/package.json` `scripts` block (after the existing `lint` entry):
    ```json
      "check:i18n": "node scripts/check-i18n-parity.mjs"
    ```
    Preserve every other entry (dev, build, build:ce, lint, preview).

    Step 5 — Extend `.github/workflows/ci.yml`. The existing `jobs.check.steps` ends with the "Lint" step (line ~22). APPEND a new step AFTER Lint:
    ```yaml
          - name: Check i18n parity
            run: npm run check:i18n -w @aquarium/web
    ```
    Preserve every other line in the file. Indentation matches the existing step pattern exactly (6 spaces for `- name:`, 12 spaces for `run:`).

    Constraints:
    - DO NOT migrate any existing z-index numbers in any CSS file besides sonner.tsx (UX3 policy: new code adopts the ladder; old code stays as-is).
    - DO NOT rename any existing CSS variable.
    - DO NOT add any new i18n keys to any locale file (plan 23-01 and onward add the `issues.board.*` namespace).
    - The parity script must pass TODAY against the current locale files — if it finds existing drift in pre-Phase-23 keys, that's a pre-existing bug and the script should print it clearly. If any drift exists, bring parity to WHATEVER en.json has — copy missing keys into other locales using the en string unchanged (marked `__missing_translation` is NOT required; the script failing once to show drift, then developer copies, is the normal flow). If the current codebase already has drift, patch ONLY what is needed to make the script green; preserve non-English translation strings where they exist.
  </action>
  <verify>
    <automated>
      grep -c "^\s*--z-" apps/web/src/index.css &&
      grep -q "\-\-z\-drag\-overlay: 5000" apps/web/src/index.css &&
      grep -q "\-\-z\-toast: 7000" apps/web/src/index.css &&
      grep -q "zIndex: 'var(--z-toast)'" apps/web/src/components/ui/sonner.tsx &&
      test -f apps/web/scripts/check-i18n-parity.mjs &&
      node apps/web/scripts/check-i18n-parity.mjs &&
      grep -q '"check:i18n"' apps/web/package.json &&
      grep -q "check:i18n" .github/workflows/ci.yml
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\s*--z-" apps/web/src/index.css` ≥ 10 (ten z-index variables added)
    - apps/web/src/index.css contains literal string `--z-drag-overlay: 5000`
    - apps/web/src/index.css contains literal string `--z-toast: 7000`
    - apps/web/src/components/ui/sonner.tsx contains `zIndex: 'var(--z-toast)'` inside the toastOptions.style object (verify adjacency with `background:` line by grep within 20 lines)
    - apps/web/src/components/ui/sonner.tsx still contains all original style fields (grep: `background:`, `border:`, `color:`, `fontSize:`, `borderRadius:`, `boxShadow:`, `padding:`, `lineHeight:`)
    - apps/web/scripts/check-i18n-parity.mjs exists, is executable as a Node script, and exits 0 when run
    - apps/web/package.json scripts object has `"check:i18n": "node scripts/check-i18n-parity.mjs"`
    - .github/workflows/ci.yml contains a step with `name: Check i18n parity` and `run: npm run check:i18n -w @aquarium/web`
  </acceptance_criteria>
  <done>
    Z-index ladder live in :root, Toaster respects it, parity script green in this repo today, CI enforces it on every push, existing styles untouched except for the sonner Toaster and index.css :root additions.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node build script → filesystem | check-i18n-parity.mjs reads locale JSON + source files; no network, no execute-arbitrary |
| npm install → lockfile | Five new deps widen supply-chain surface; versions pinned per 23-RESEARCH §Standard Stack (npm registry verified 2026-04-17) |
| CI job → repo | New CI step runs inside GitHub Actions sandbox; no new secrets, no new network calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-00-01 | Tampering | npm deps pinned range | accept | `^x.y.z` range is the npm norm; lockfile provides reproducibility. Research-verified versions (2026-04-17). |
| T-23-00-02 | Information Disclosure | check-i18n-parity.mjs reading locale JSON | accept | Locale files are source-committed and not secret. Script uses only Node built-ins — no eval, no dynamic require. |
| T-23-00-03 | Denial of Service | CI step adds ~2 s to every push | accept | Parity script runtime dominated by fs.globSync; expected < 2 s on a 1000-file repo. |
| T-23-00-04 | Elevation of Privilege | Playwright spec scaffold shipping empty test bodies | accept | `test.skip()` is harmless — the spec file cannot run DB writes or network calls until plans 01–04 wire real code. |
| T-23-00-05 | Tampering | WsMessage.payload going from required to optional | mitigate | Backward-compatible change for TypeScript consumers (payload still typed to Record<string, unknown>), and all existing server emitters set payload. Verify `npm run typecheck -w @aquaclawai/aquarium` passes — task is complete only if existing code still compiles. |
| T-23-00-06 | Spoofing | Adding WS event literals to the shared union | accept | Purely additive to a string-literal union — no runtime change. Server continues to emit whatever it already emits. |

</threat_model>

<verification>
- `npm install -w @aquarium/web` completed and 5 deps appear in package.json dependencies
- `grep` checks in `<verify>` for every task all return the matches named in <acceptance_criteria>
- `node apps/web/scripts/check-i18n-parity.mjs` exits 0 against the current repo
- CI YAML parses (GitHub Actions validation happens on push; local check: `grep -A1 "check:i18n" .github/workflows/ci.yml` shows the step shape)
- `npm run typecheck -w @aquaclawai/aquarium` passes (verifies WsEventType extension didn't break server code)
- No existing CSS z-index number was changed (grep for any `z-index:` in CSS files other than sonner.tsx returns unchanged)
</verification>

<success_criteria>
- 5 pinned npm deps installed
- Z-index ladder established (10 CSS variables)
- Toaster uses `var(--z-toast)`
- i18n parity script exists, green today, enforced in CI
- WsEventType union extended to cover issue:* + task:cancelled
- Component directory scaffolded
- Playwright spec stubbed with 7 skip-scenario names downstream plans remove
- A1 WS semantics finding documented so plan 23-01 wires `subscribe('AQ')` correctly
- No existing feature code is modified beyond the Toaster zIndex add and the shared WsEventType additive change
</success_criteria>

<output>
After completion, create `.planning/phases/23-issue-board-ui-kanban/23-00-SUMMARY.md` covering:
- Exact deps installed with versions resolved by npm
- Confirm z-index ladder visible and Toaster style updated
- Note whether the parity script caught any pre-existing locale drift + what was patched (if any)
- A1 finding reiterated in one sentence so plan 23-01 can cite it
</output>
