---
phase: 25-management-uis
plan: 04
type: execute
wave: 4
depends_on: [25-01, 25-02, 25-03]
files_modified:
  - apps/web/src/i18n/locales/zh.json
  - apps/web/src/i18n/locales/fr.json
  - apps/web/src/i18n/locales/de.json
  - apps/web/src/i18n/locales/es.json
  - apps/web/src/i18n/locales/it.json
  - .planning/PROJECT.md
autonomous: true
requirements: [MGMT-01, MGMT-02, MGMT-03]

must_haves:
  truths:
    - "All new `management.agents.*` / `management.runtimes.*` / `management.daemonTokens.*` + `sidebar.agents|runtimes|daemonTokens` keys translated in zh/fr/de/es/it"
    - "No English placeholder values remain in the 5 non-English locale files for these namespaces"
    - "i18n parity script still exits 0"
    - "PROJECT.md evolution footer updated to note Phase 25 completion"
  artifacts:
    - path: "apps/web/src/i18n/locales/zh.json"
      provides: "Simplified Chinese translations for management.* + sidebar additions"
    - path: "apps/web/src/i18n/locales/fr.json"
      provides: "French translations"
    - path: "apps/web/src/i18n/locales/de.json"
      provides: "German translations"
    - path: "apps/web/src/i18n/locales/es.json"
      provides: "Spanish translations"
    - path: "apps/web/src/i18n/locales/it.json"
      provides: "Italian translations"
    - path: ".planning/PROJECT.md"
      provides: "Evolution footer updated with Phase 25 completion date"
  key_links:
    - from: "apps/web/src/i18n/locales/{zh,fr,de,es,it}.json"
      to: "apps/web/scripts/check-i18n-parity.mjs"
      via: "parity gate green with all 6 locales in key-structure match"
      pattern: "OK: [0-9]+ keys across 6 locales"
---

<objective>
UI-08 carry-forward: translate `management.agents.*` + `management.runtimes.*` + `management.daemonTokens.*` + 3 new `sidebar.*` keys in zh/fr/de/es/it. Wave 0 shipped en-complete + 5-locale English placeholders so CI stayed green through Waves 1-3; this wave replaces the placeholders with real translations.

Purpose: Complete MGMT-01/02/03 requirements by shipping all strings in all 6 locales (enforced by existing CI parity gate from Phase 23 + reinforced by Phase 23 + 24 translation quality standard).

Output:
- 5 updated locale files with real translations (Chinese / French / German / Spanish / Italian) for every new key added in Wave 0
- PROJECT.md evolution footer noting Phase 25 completion
- i18n parity script continues to pass
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-management-uis/25-UI-SPEC.md
@.planning/phases/25-management-uis/25-VALIDATION.md
@.planning/phases/25-management-uis/25-00-SUMMARY.md
@.planning/phases/25-management-uis/25-01-SUMMARY.md
@.planning/phases/25-management-uis/25-02-SUMMARY.md
@.planning/phases/25-management-uis/25-03-SUMMARY.md
@.planning/phases/23-issue-board-ui-kanban/23-05-SUMMARY.md
@.planning/phases/24-issue-detail-ui-task-message-streaming/24-06-SUMMARY.md
@apps/web/src/i18n/locales/en.json
@apps/web/src/i18n/locales/zh.json
@apps/web/src/i18n/locales/fr.json
@apps/web/src/i18n/locales/de.json
@apps/web/src/i18n/locales/es.json
@apps/web/src/i18n/locales/it.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate management.* + sidebar additions to zh/fr/de/es/it</name>
  <files>
    apps/web/src/i18n/locales/zh.json
    apps/web/src/i18n/locales/fr.json
    apps/web/src/i18n/locales/de.json
    apps/web/src/i18n/locales/es.json
    apps/web/src/i18n/locales/it.json
  </files>
  <read_first>
    - apps/web/src/i18n/locales/en.json (FULL management.* + sidebar.{agents,runtimes,daemonTokens} — the source of truth)
    - apps/web/src/i18n/locales/zh.json + fr.json + de.json + es.json + it.json (current state — English placeholders for new keys)
    - .planning/phases/23-issue-board-ui-kanban/23-05-SUMMARY.md (translation quality patterns for Issue Board; tone + length conventions)
    - .planning/phases/24-issue-detail-ui-task-message-streaming/24-06-SUMMARY.md (translation quality patterns for Issue Detail; how interpolation variables are preserved in each locale)
  </read_first>
  <action>
    For each of the 5 locale files (zh / fr / de / es / it) — preserve ALL pre-existing non-English translations byte-for-byte. ONLY modify:

    **(a) sidebar nav additions (3 keys):**
    - `sidebar.agents` — zh: `代理` / fr: `Agents` / de: `Agenten` / es: `Agentes` / it: `Agenti`
    - `sidebar.runtimes` — zh: `运行环境` / fr: `Environnements` / de: `Laufzeiten` / es: `Entornos` / it: `Runtime`
    - `sidebar.daemonTokens` — zh: `守护进程令牌` / fr: `Jetons de démon` / de: `Daemon-Tokens` / es: `Tokens de daemon` / it: `Token daemon`

    **(b) `management.agents.*` namespace (~70 keys)** — translate every string currently equal to the English placeholder. Pay attention to i18next interpolation variables (`{{name}}`, `{{count}}`, `{{max}}`, `{{key}}`, `{{arg}}`, `{{retry}}`) — these MUST be preserved verbatim across locales. Example pattern (Chinese):
    - `title` → `代理`
    - `description` → `代理定义需要完成的工作以及完成方式。将代理分配给某个事项后即可开始任务。`
    - `actions.create` → `新建代理`
    - `tabs.active` → `活跃`
    - `tabs.archived` → `已归档`
    - `form.titleCreate` → `创建代理`
    - `form.titleEdit` → `编辑代理`
    - `form.name.label` → `名称`
    - `form.instructions.label` → `指令`
    - `form.instructions.counter` → `{{count}} / {{max}} 字符` (preserve interpolation)
    - `archiveConfirm.title` → `归档 "{{name}}" 吗？` (preserve `{{name}}`)
    - `form.customEnv.duplicateKey` → `键 "{{key}}" 重复 — 仅保留最后一个值`

    Apply analogous translations for fr / de / es / it. Use natural, concise translations matching the tone established in Phase 23 + 24 locales.

    **(c) `management.runtimes.*` namespace (~35 keys)** — translate every string. Interpolation variables to preserve: `{{count}}`, `{{relativeTime}}`, `{{absoluteTime}}`, `{{name}}`, `{{status}}`.

    **(d) `management.daemonTokens.*` namespace (~40 keys)** — translate every string. Interpolation variables: `{{name}}`. CRITICAL: `a11y.copied` MUST translate to the local equivalent of "Token copied to clipboard" WITHOUT any `{{plaintext}}` interpolation — this invariant is grep-enforced. Example:
    - zh: `令牌已复制到剪贴板`
    - fr: `Jeton copié dans le presse-papiers`
    - de: `Token in Zwischenablage kopiert`
    - es: `Token copiado al portapapeles`
    - it: `Token copiato negli appunti`

    **Quality rules (from Phase 23 + 24 patterns):**
    1. Preserve ALL `{{variable}}` interpolations exactly — same name, same position where possible within natural sentence structure
    2. Preserve trailing ellipses (`…`) and em-dashes as-is
    3. Keep destructive confirmations clear — e.g., German `"{{name}}" widerrufen?` / Spanish `¿Revocar "{{name}}"?`
    4. Keep button verbs imperative — e.g., Chinese `归档` / German `Archivieren` / Italian `Archivia`
    5. For copy-once warning callout: preserve the sense "this is the only time you'll see this token — copy it now" in each locale
    6. Do NOT invent new keys. Do NOT drop existing keys. ONLY translate values.

    **Translation approach:**
    - Produce the full translation for all 5 locales in one pass
    - Cross-reference Phase 23 + 24 locale files for already-translated shared terms (e.g., `common.buttons.cancel` is already translated — reuse reference, don't re-translate)
    - Use native speaker-quality phrasing; do not machine-translate literal English idioms
    - Flag any ambiguous original English strings in the SUMMARY for human linguistic review (manual-only verification per VALIDATION.md)

    File-editing strategy: use JSON-safe Edit operations to replace each English placeholder with its translation; do not rewrite the full file (preserves prior phases' translations).
  </action>
  <verify>
    <automated>node apps/web/scripts/check-i18n-parity.mjs &amp;&amp; bash -c 'if grep -rnE "\\{\\{plaintext" apps/web/src/i18n/locales/ 2&gt;/dev/null; then exit 1; fi; exit 0' &amp;&amp; npm run build:ce -w @aquarium/web</automated>
  </verify>
  <acceptance_criteria>
    - `node apps/web/scripts/check-i18n-parity.mjs` exits 0
    - Every `management.daemonTokens.a11y.copied` in all 6 locales does NOT contain `{{plaintext}}` — HARD invariant grep: `grep -rnE "\\{\\{plaintext" apps/web/src/i18n/locales/` returns 0
    - Every `{{variable}}` interpolation present in en.json is present in zh.json + fr.json + de.json + es.json + it.json (parity of placeholders, not just keys)
    - zh/fr/de/es/it `sidebar.agents|runtimes|daemonTokens` are translated to the locale (not English)
    - `zh.json` no longer contains the literal English strings `"Agents"` / `"Runtimes"` / `"Daemon Tokens"` for the sidebar entries
    - Full Playwright suite still green: `npx playwright test tests/e2e/management-uis.spec.ts` exits 0
    - Build still green: `npm run build:ce -w @aquarium/web` exits 0
    - Pre-existing translations in zh/fr/de/es/it for Phases 1-24 preserved byte-for-byte outside `management.*` + `sidebar.agents|runtimes|daemonTokens`
  </acceptance_criteria>
  <done>
    All 5 non-English locale files contain native translations for every key added in Wave 0. i18n parity green. MGMT-03 HARD plaintext a11y invariant preserved across all locales. Playwright suite + web build + lint all green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update PROJECT.md evolution footer with Phase 25 completion</name>
  <files>
    .planning/PROJECT.md
  </files>
  <read_first>
    - .planning/PROJECT.md (FULL — understand the existing evolution footer format)
    - .planning/phases/24-issue-detail-ui-task-message-streaming/24-06-SUMMARY.md (Phase 24's final evolution footer entry pattern)
  </read_first>
  <action>
    Append a Phase 25 completion note to the project evolution footer in the same style as prior phase entries. Include:
    - Date (today's date via commit timestamp)
    - Phase name: `Phase 25: Management UIs`
    - Requirements closed: `MGMT-01 (agents CRUD + archive), MGMT-02 (unified runtimes list + filter + detail drawer), MGMT-03 (daemon tokens with copy-once plaintext)`
    - 1-2 sentence delta summary (e.g., "Completed /agents, /runtimes, /daemon-tokens management pages with plaintext-once copy security invariants for daemon tokens. All strings localized across 6 locales; CI grep-guards enforce XSS + storage invariants on the management subtree.")
    - Link to the 4 SUMMARY files: `25-00-SUMMARY.md`, `25-01-SUMMARY.md`, `25-02-SUMMARY.md`, `25-03-SUMMARY.md`, `25-04-SUMMARY.md`

    Preserve all existing footer content byte-for-byte. Do NOT rewrite earlier entries.

    If PROJECT.md has no evolution footer, read the file first to understand the expected pattern from Phase 24's entry and add a new section.
  </action>
  <verify>
    <automated>grep -c "Phase 25" .planning/PROJECT.md &amp;&amp; grep -c "MGMT-01\|MGMT-02\|MGMT-03" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - PROJECT.md references `Phase 25` and all three requirement IDs
    - PROJECT.md byte-for-byte preserves pre-existing content outside the new footer entry
  </acceptance_criteria>
  <done>
    PROJECT.md evolution footer carries forward the project narrative through Phase 25. Ready for Phase 26 (integration, boot wiring, E2E, release).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| locale files → UI rendering | Translated strings rendered as React text children (auto-escaped); i18next interpolation `{{var}}` is parameterized — not string concatenation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04-01 | Information Disclosure (plaintext-in-a11y regression during translation) | apps/web/src/i18n/locales/*.json | mitigate | HARD grep invariant re-checked after this wave: `rg "\\{\\{plaintext" apps/web/src/i18n/locales/` returns 0. If a translator accidentally adds `{{plaintext}}`, CI + task acceptance fail. |
| T-25-04-02 | Tampering (interpolation placeholder mismatch) | apps/web/src/i18n/locales/*.json | mitigate | Acceptance criterion requires per-locale interpolation parity with en.json — same `{{var}}` names preserved. Prevents React render errors from missing placeholders. |
| T-25-04-03 | Repudiation (translation quality) | zh/fr/de/es/it locale files | transfer | Manual linguistic review noted in VALIDATION.md as a native-speaker-only verification — cannot be automated |
| T-25-04-04 | DoS via oversized translation strings | Locale files | accept | All strings bounded by UI layout (table cells, dialog widths); no server processing of locale values |
</threat_model>

<verification>
Wave 4 green when:
1. `node apps/web/scripts/check-i18n-parity.mjs` exits 0
2. HARD invariant: `grep -rnE "\\{\\{plaintext" apps/web/src/i18n/locales/` returns 0
3. Placeholder parity: every locale contains the same set of `{{name}}` / `{{count}}` / `{{max}}` / `{{key}}` / `{{arg}}` / `{{retry}}` / `{{relativeTime}}` / `{{absoluteTime}}` / `{{status}}` placeholders that en.json uses
4. `npm run build:ce -w @aquarium/web` exits 0
5. Full Playwright suite: `npx playwright test tests/e2e/management-uis.spec.ts` — 9 passed (0 skipped)
6. Visual smoke (manual, per VALIDATION.md): switch language in UI, verify zh/fr/de/es/it render correctly on /agents, /runtimes, /daemon-tokens
</verification>

<success_criteria>
- All 3 requirements (MGMT-01, MGMT-02, MGMT-03) close with full 6-locale coverage
- UI-08 carry-forward honoured — no placeholders remain
- Phase 25 complete; ready for Phase 26 integration work
</success_criteria>

<output>
After completion, create `.planning/phases/25-management-uis/25-04-SUMMARY.md` documenting:
- 5 locale files translated (with approximate key-count and any specific linguistic decisions)
- PROJECT.md updated
- Final i18n parity count (`OK: N keys across 6 locales`)
- Confirmation of HARD MGMT-03 plaintext-in-a11y invariant across all locales
- Phase 25 final status: 4 waves complete, 5 plans complete, 3 requirements closed
</output>
