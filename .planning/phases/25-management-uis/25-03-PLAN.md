---
phase: 25-management-uis
plan: 03
type: execute
wave: 3
depends_on: [25-00]
files_modified:
  - apps/web/src/pages/DaemonTokensPage.tsx
  - apps/web/src/components/management/DaemonTokenList.tsx
  - apps/web/src/components/management/DaemonTokenCreateModal.tsx
  - apps/web/src/components/management/RevokeConfirmDialog.tsx
  - apps/web/src/components/management/useDaemonTokens.ts
  - apps/web/src/components/management/tokenStatus.ts
  - tests/e2e/management-uis.spec.ts
autonomous: true
requirements: [MGMT-03]

must_haves:
  truths:
    - "User can create a daemon token with a friendly name and optional expiry date"
    - "Plaintext adt_ token shown EXACTLY ONCE in a Dialog; copy + dismiss works; plaintext disappears from DOM after dismiss"
    - "List page shows all tokens with derived status badges — NEVER plaintext and NEVER the token hash"
    - "Revoke flow requires an explicit destructive-variant confirmation dialog"
  artifacts:
    - path: "apps/web/src/pages/DaemonTokensPage.tsx"
      provides: "Route component for /daemon-tokens"
      contains: "data-page=\"daemon-tokens\""
    - path: "apps/web/src/components/management/DaemonTokenList.tsx"
      provides: "shadcn Table with token rows + derived status badges"
      contains: "data-token-row"
    - path: "apps/web/src/components/management/DaemonTokenCreateModal.tsx"
      provides: "Two-step Dialog; plaintext in local useState only"
      contains: "data-token-plaintext"
    - path: "apps/web/src/components/management/RevokeConfirmDialog.tsx"
      provides: "Destructive-variant confirmation Dialog"
      contains: "data-token-revoke-confirm"
    - path: "apps/web/src/components/management/useDaemonTokens.ts"
      provides: "Fetch + revoke hook — plaintext NEVER passes through"
    - path: "apps/web/src/components/management/tokenStatus.ts"
      provides: "deriveTokenStatus pure function"
    - path: "tests/e2e/management-uis.spec.ts"
      provides: "Un-skipped: token create form, token copy once, token revoke"
  key_links:
    - from: "apps/web/src/components/management/DaemonTokenCreateModal.tsx"
      to: "/api/daemon-tokens (POST)"
      via: "api.post<DaemonTokenCreatedResponse>('/daemon-tokens', { name, expiresAt })"
      pattern: "api.post<DaemonTokenCreatedResponse>"
    - from: "apps/web/src/components/management/DaemonTokenCreateModal.tsx"
      to: "useState<string | null>(null)"
      via: "Plaintext only here; cleared on dismiss"
      pattern: "useState<string \\| null>"
    - from: "apps/web/src/components/management/RevokeConfirmDialog.tsx"
      to: "/api/daemon-tokens/:id (DELETE)"
      via: "api.delete via useDaemonTokens.revoke"
      pattern: "api.delete"
---

<objective>
MGMT-03: issue, list, revoke daemon tokens. THE hard security invariant: plaintext `adt_<32>` token shown EXACTLY ONCE on creation, lives only in local React state, never touches localStorage / sessionStorage / URL / document.title / console / a11y announcer / logs. List endpoint never returns plaintext (type-enforced via `DaemonToken` projection). Revoke requires destructive confirmation.

Purpose: This plan owns the highest-severity threat in Phase 25 (T-25-03-01 plaintext daemon token leak). CI grep guards from Plan 25-00 enforce structural invariants in perpetuity.

Output:
- Fully wired `/daemon-tokens` page with list, two-step create modal (form → copy-once view), and revoke confirmation dialog
- 6 new files under `apps/web/src/components/management/`
- 3 Playwright scenarios un-skipped and green: `token create form`, `token copy once`, `token revoke`

EXPLICIT grep-verifiable invariants for MGMT-03 HARD (run at every task):
- `grep -rnE "localStorage|sessionStorage" apps/web/src/components/management/DaemonTokenCreateModal.tsx` → 0 matches
- `grep -rn "dangerouslySetInnerHTML" apps/web/src/components/management/` → 0 matches
- `grep -n "document\\.title" apps/web/src/components/management/DaemonTokenCreateModal.tsx` → 0 matches
- `grep -n "console\\." apps/web/src/components/management/DaemonTokenCreateModal.tsx` → 0 matches (Warning-4 revision fix)
- `grep -rnE "\\{\\{plaintext" apps/web/src/i18n/locales/` → 0 matches (a11y copy NEVER interpolates plaintext)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-management-uis/25-UI-SPEC.md
@.planning/phases/25-management-uis/25-VALIDATION.md
@.planning/phases/25-management-uis/25-00-SUMMARY.md
@.planning/phases/19-daemon-rest-api-auth/19-03-SUMMARY.md
@apps/server/src/routes/daemon-tokens.ts
@apps/web/src/api.ts
@apps/web/src/components/ui/dialog.tsx
@apps/web/src/components/ui/input.tsx
@apps/web/src/components/ui/popover.tsx
@apps/web/src/components/ui/table.tsx
@apps/web/src/components/ui/badge.tsx
@packages/shared/src/v14-types.ts

<interfaces>
Server endpoints (shipped from Phase 19-03 — apps/server/src/routes/daemon-tokens.ts):
- POST /api/daemon-tokens body `{ name: string; expiresAt?: string | null }` → ApiResponse<DaemonTokenCreatedResponse> — ONLY endpoint returning plaintext
- GET /api/daemon-tokens → ApiResponse<DaemonToken[]> — projection, NO plaintext, NO tokenHash
- DELETE /api/daemon-tokens/:id → ApiResponse<{ ok: boolean }> — idempotent soft revoke

Shared types:
```ts
export interface DaemonToken {
  id: string; workspaceId: string; name: string;
  daemonId: string | null; createdByUserId: string | null;
  expiresAt: string | null; lastUsedAt: string | null; revokedAt: string | null;
  createdAt: string; updatedAt: string;
}
export interface DaemonTokenCreatedResponse { token: DaemonToken; plaintext: string; }
```
Type-level invariant: `DaemonToken` has NO plaintext field.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tokenStatus helper + useDaemonTokens hook + DaemonTokenList + DaemonTokensPage read-only slice</name>
  <files>
    apps/web/src/components/management/tokenStatus.ts
    apps/web/src/components/management/useDaemonTokens.ts
    apps/web/src/components/management/DaemonTokenList.tsx
    apps/web/src/pages/DaemonTokensPage.tsx
  </files>
  <read_first>
    - .planning/phases/25-management-uis/25-UI-SPEC.md lines 186-192 (Per-daemon-token-status derivation)
    - .planning/phases/25-management-uis/25-UI-SPEC.md lines 414-504 (management.daemonTokens.* i18n)
    - .planning/phases/25-management-uis/25-UI-SPEC.md lines 571-580 (Component Inventory)
    - .planning/phases/25-management-uis/25-UI-SPEC.md lines 850-864 (Data-attribute markers)
    - apps/server/src/routes/daemon-tokens.ts (FULL)
    - apps/web/src/components/ui/badge.tsx + table.tsx + dropdown-menu.tsx
  </read_first>
  <behavior>
    - Test: deriveTokenStatus returns 'active' when revokedAt=null and expiresAt=null
    - Test: deriveTokenStatus returns 'expiring_soon' when expiresAt is within 7 days
    - Test: deriveTokenStatus returns 'expired' when expiresAt is in the past
    - Test: deriveTokenStatus returns 'revoked' when revokedAt is non-null (regardless of expiresAt)
  </behavior>
  <action>
    **1. tokenStatus.ts** — Pure named export `deriveTokenStatus(token: DaemonToken, now: Date = new Date()): 'active' | 'expiring_soon' | 'expired' | 'revoked'`. Rules from UI-SPEC lines 186-192:
    ```ts
    export type DaemonTokenDerivedStatus = 'active' | 'expiring_soon' | 'expired' | 'revoked';
    export function deriveTokenStatus(token: DaemonToken, now: Date = new Date()): DaemonTokenDerivedStatus {
      if (token.revokedAt !== null) return 'revoked';
      if (token.expiresAt !== null) {
        const exp = new Date(token.expiresAt);
        if (exp <= now) return 'expired';
        const SEVEN_DAYS_MS = 7 * 24 * 60 * 60 * 1000;
        if (exp.getTime() - now.getTime() <= SEVEN_DAYS_MS) return 'expiring_soon';
      }
      return 'active';
    }
    ```
    Pure — no side effects. Testable in isolation.

    **2. useDaemonTokens.ts** — Named export returning `{ tokens, isLoading, error, refetch, revoke }`. Uses `api.get<DaemonToken[]>('/daemon-tokens')` on mount + `revoke(id)` calling `api.delete<{ ok: boolean }>('/daemon-tokens/' + id)` + refetch. NO `create` method — plaintext cannot pass through this hook.

    **3. DaemonTokenList.tsx** — Props `{ tokens: DaemonToken[]; isLoading: boolean; onRevoke: (t: DaemonToken) => void }`. Shadcn `<Table>` with columns Name / Created / Expires / LastUsed / Status / sr-only Actions. Each row `<tr data-token-row={token.id} data-token-status={deriveTokenStatus(token, now)}>`:
    - Name: `{token.name}` in `font-medium` — React text child (no innerHTML)
    - Created: `formatRelativeTime(token.createdAt, locale)` + Tooltip with absolute time
    - Expires: `neverExpires` if null, else absolute date
    - LastUsed: `neverUsed` if null, else relative + Tooltip
    - Status: `<Badge>` per derived status:
      - `active` → success-subtle + `<CheckCircle2>` + `status.active`
      - `expiring_soon` → warning-subtle + `<Clock>` + `status.expiringSoon`
      - `expired` → `variant="destructive"` + `<AlertTriangle>` + `status.expired`
      - `revoked` → `variant="outline"` + `text-muted-foreground opacity-70` + `<ShieldOff>` + `status.revoked`
    - Actions: shadcn `<DropdownMenu>` with Revoke item `data-token-revoke-open={token.id}` → `onRevoke(token)`. Disabled when derivedStatus === 'revoked'.

    Loading: 5 skeleton rows. Empty: `<EmptyState icon={KeyRound} heading={t('management.daemonTokens.empty.heading')} body={...} cta={{ label: t('management.daemonTokens.empty.cta'), onClick: onOpenCreate }}>`.

    **4. DaemonTokensPage.tsx** — Replace Wave 0 stub:
    - `const { tokens, isLoading, error, refetch, revoke } = useDaemonTokens()`
    - `const [createOpen, setCreateOpen] = useState(false)` — Task 2 wires
    - `const [revokeTarget, setRevokeTarget] = useState<DaemonToken | null>(null)` — Task 3 wires
    - `<main data-page="daemon-tokens" className="mx-auto max-w-[1200px] p-6 pb-8">` with h1, description, toolbar containing `<Button data-token-create-open onClick={() => setCreateOpen(true)}>{t('management.daemonTokens.actions.create')}</Button>`, then `<DaemonTokenList>` wired with `onRevoke={(t) => setRevokeTarget(t)}`
    - sr-only `<div role="status" aria-live="polite" className="sr-only">{announcement}</div>` — **CRITICAL**: announcement NEVER contains plaintext; only uses `management.daemonTokens.a11y.*` keys.
  </action>
  <verify>
    <automated>npm run build -w @aquarium/shared &amp;&amp; npm run typecheck -w @aquaclawai/aquarium &amp;&amp; npm run build:ce -w @aquarium/web &amp;&amp; node apps/web/scripts/check-i18n-parity.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 4 new files created
    - `grep -c "data-token-row" apps/web/src/components/management/DaemonTokenList.tsx` returns `>=1`
    - `grep -c "data-token-status" apps/web/src/components/management/DaemonTokenList.tsx` returns `>=1`
    - `grep -c "api.get<DaemonToken\\[\\]>" apps/web/src/components/management/useDaemonTokens.ts` returns `1`
    - `grep -c "api.delete" apps/web/src/components/management/useDaemonTokens.ts` returns `>=1`
    - `grep -c "plaintext" apps/web/src/components/management/useDaemonTokens.ts` returns 0
    - `grep -rn "dangerouslySetInnerHTML" apps/web/src/components/management/` returns 0
    - `grep -rnE "localStorage|sessionStorage" apps/web/src/components/management/` returns 0
  </acceptance_criteria>
  <done>
    Read-only /daemon-tokens page with derived status badges renders correctly. Hook fetches via /api/daemon-tokens; plaintext never passes through. Playwright scenarios still skipped — Task 2 un-skips two; Task 3 un-skips revoke.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DaemonTokenCreateModal (two-step + MGMT-03 HARD invariants) + token create form + token copy once scenarios</name>
  <files>
    apps/web/src/components/management/DaemonTokenCreateModal.tsx
    apps/web/src/pages/DaemonTokensPage.tsx
    tests/e2e/management-uis.spec.ts
  </files>
  <read_first>
    - .planning/phases/25-management-uis/25-UI-SPEC.md lines 457-495 (Create modal i18n keys)
    - .planning/phases/25-management-uis/25-UI-SPEC.md lines 576-580 (DaemonTokenCreateModal contract — state machine local; plaintext never escapes callback)
    - .planning/phases/25-management-uis/25-UI-SPEC.md lines 676-692 (Clipboard contract + fallback)
    - .planning/phases/25-management-uis/25-UI-SPEC.md lines 720-750 (Security Contract MGMT-03 HARD invariants table — including NEVER-logged-to-console row line 731)
    - .planning/phases/25-management-uis/25-UI-SPEC.md lines 885-896 (Two-step interaction flow + confirm-close)
    - apps/server/src/routes/daemon-tokens.ts POST handler
    - apps/web/src/components/ui/dialog.tsx + input.tsx + popover.tsx
  </read_first>
  <behavior>
    - Test: Click New token → Step A visible with `[data-token-form-field="name"]` focused
    - Test: Submit empty name → inline required error; POST not called
    - Test: Submit with valid name + expiry → Step B visible with `[data-token-plaintext]` starting with `adt_`
    - Test: Click Copy → navigator.clipboard.writeText called with plaintext; sr-announcer fires `"Token copied to clipboard"` WITHOUT plaintext
    - Test: Click `[data-token-dismiss]` → modal closes, plaintext element gone from DOM, list row visible with `[data-token-status="active"]`
    - Test: Press Escape on Step B → ConfirmClose dialog appears; cancel returns to Step B; confirm dismisses
  </behavior>
  <action>
    **1. DaemonTokenCreateModal.tsx** — Named export `{ open: boolean; onOpenChange: (open: boolean) => void; onCreated: (token: DaemonToken) => void }`.

    **STRUCTURAL SECURITY CONTRACT:**
    - `const [plaintext, setPlaintext] = useState<string | null>(null);` — ONLY place plaintext lives
    - `const [step, setStep] = useState<'form' | 'copy-once'>('form');`
    - `const [confirmCloseOpen, setConfirmCloseOpen] = useState(false);`
    - `const [copyState, setCopyState] = useState<'idle'|'copied'|'failed'>('idle');`
    - Cleanup: `function dismissCopyOnce() { setPlaintext(null); setStep('form'); setCopyState('idle'); onOpenChange(false); }`
    - `onCreated(resp.token)` ONLY — never `resp.plaintext` (type-enforced: DaemonToken has no plaintext)
    - **Forbidden (structural, grep-enforced):** no `localStorage` / `sessionStorage` / `document.title` mutation / **ANY `console.*` calls at all** (Warning-4 fix — no `console.log`, `console.warn`, `console.error`, `console.debug`, `console.info`, `console.trace`, etc.) / `history.pushState` with plaintext / i18n `{{plaintext}}` interpolation

    **Step A — form:**
    Shadcn Dialog intercepting close on Step B:
    ```tsx
    <Dialog open={open} onOpenChange={(o) => {
      if (!o && step === 'copy-once' && plaintext !== null) {
        setConfirmCloseOpen(true);
        return;  // block the close
      }
      if (!o) dismissCopyOnce();
      else onOpenChange(true);
    }}>
    ```
    `<DialogContent className="max-w-[520px]">` with:
    - Header: `<DialogTitle>{t('management.daemonTokens.createModal.title')}</DialogTitle>`
    - Body `space-y-4 p-6`:
      - `<Input data-token-form-field="name" autoFocus maxLength={100}>` + hint + required/tooLong inline errors
      - `<Popover>` for expiry with `<input type="date" min={todayISO}>` trigger — `data-token-form-field="expiresAt"`. Clear button sets expiry to null. Future-date validation.
    - Footer: Cancel (outline) + `<Button data-token-form-submit disabled={isSubmitting}>` = `actions.create` / `actions.creating`
    - Submit:
      ```ts
      try {
        const resp = await api.post<DaemonTokenCreatedResponse>('/daemon-tokens', { name, expiresAt });
        setPlaintext(resp.plaintext);          // the only line touching plaintext
        setStep('copy-once');
        onCreated(resp.token);                 // projection only
      } catch (e) {
        // IMPORTANT: do NOT `console.error(e)` — plaintext must not reach console even indirectly.
        // Toast copy comes from i18n; the underlying ApiError details stay within the throw chain.
        toast.error(t('management.daemonTokens.createModal.createFailed'));
      }
      ```

    **Step B — copy-once view:**
    When `step === 'copy-once'`:
    - Header: `<DialogTitle>{t('...copyOnce.title')}</DialogTitle>`
    - Warning callout: `<div className="p-3 bg-[var(--color-warning-subtle-bg)] text-[var(--color-warning-subtle-text)] rounded flex gap-2 items-start">` with `<AlertTriangle>` + aria-label `warningIcon` + `{t('...copyOnce.warning')}`
    - Token block: `<pre data-token-plaintext className="my-4 p-4 text-xs font-mono bg-muted rounded select-all break-all">{plaintext}</pre>` — React text child, select-all for manual copy
    - Footer with 2 buttons:
      - `<Button data-token-copy-button variant={copyState === 'copied' ? 'secondary' : 'default'} onClick={handleCopy}>` text = `copyButton` / `copied` / `copyFailed` per copyState
      - `<Button data-token-dismiss variant="outline" onClick={dismissCopyOnce}>{t('...copyOnce.dismiss')}</Button>`
    - handleCopy (IMPORTANT: no console.* anywhere in this handler):
      ```ts
      async function handleCopy() {
        if (plaintext === null) return;
        try {
          await navigator.clipboard.writeText(plaintext);
          setCopyState('copied');
          // NOTE: a11y announcement goes via parent's announcement state — NEVER includes plaintext
          // NOTE: NO console.* logging — plaintext must not reach console even indirectly
        } catch {
          // No console.error — catch swallowed; UI state transitions to 'failed' so user knows
          setCopyState('failed');
        }
      }
      ```
    - Confirm-close nested Dialog (rendered conditionally): shadcn Dialog with title `confirmClose.title`, body `confirmClose.body`, buttons: Keep-showing (cancel, default variant, autoFocus) + Close-anyway (destructive) → `dismissCopyOnce()` + `setConfirmCloseOpen(false)`

    **2. DaemonTokensPage.tsx** — render `<DaemonTokenCreateModal open={createOpen} onOpenChange={setCreateOpen} onCreated={(token) => { refetch(); setAnnouncement(t('management.daemonTokens.a11y.created', { name: token.name })); }}>`. Announcement (sr-only div) NEVER contains plaintext — only `{{name}}`.

    **3. tests/e2e/management-uis.spec.ts** — un-skip 2 scenarios:

    Scenario `'token create form'`:
    - Navigate `/daemon-tokens`, click `[data-token-create-open]`, assert Step A visible
    - Fill `[data-token-form-field="name"]` with `E2E-Token-${Date.now()}`, set expiry 30 days ahead via the date input
    - Click `[data-token-form-submit]` → assert `[data-token-plaintext]` becomes visible

    Scenario `'token copy once'` (MGMT-03 HARD):
    - `page.addInitScript` BEFORE `page.goto`: `(window as any).__clipboardWrites = []; const orig = navigator.clipboard.writeText.bind(navigator.clipboard); navigator.clipboard.writeText = async (v) => { (window as any).__clipboardWrites.push(v); return orig(v); };`
    - Navigate `/daemon-tokens`, click `[data-token-create-open]`, fill name `E2E-CopyOnce-${Date.now()}`, submit
    - Capture plaintext text: `const pt = await page.locator('[data-token-plaintext]').innerText(); expect(pt).toMatch(/^adt_/);`
    - Click `[data-token-copy-button]`
    - `const writes = await page.evaluate(() => (window as any).__clipboardWrites); expect(writes[0]).toBe(pt);`
    - Click `[data-token-dismiss]`
    - Assert `[data-token-plaintext]` is absent from DOM
    - Reload the page → assert row visible with `[data-token-status="active"]` AND page body HTML does NOT contain the plaintext string AND localStorage + sessionStorage (via `page.evaluate`) contain no entry whose key or value equals the plaintext
  </action>
  <verify>
    <automated>npm run build -w @aquarium/shared &amp;&amp; npm run typecheck -w @aquaclawai/aquarium &amp;&amp; npm run build:ce -w @aquarium/web &amp;&amp; bash -c 'if grep -rnE "localStorage|sessionStorage" apps/web/src/components/management/DaemonTokenCreateModal.tsx 2&gt;/dev/null; then exit 1; fi; if grep -rn "dangerouslySetInnerHTML" apps/web/src/components/management/ 2&gt;/dev/null; then exit 1; fi; if grep -n "document\\.title" apps/web/src/components/management/DaemonTokenCreateModal.tsx 2&gt;/dev/null; then exit 1; fi; exit 0' &amp;&amp; ! grep -n "console\\." apps/web/src/components/management/DaemonTokenCreateModal.tsx &amp;&amp; npx playwright test tests/e2e/management-uis.spec.ts -g "token create form|token copy once"</automated>
  </verify>
  <acceptance_criteria>
    - 1 new file + 1 modified + 1 spec un-skipped
    - `grep -c "data-token-plaintext" apps/web/src/components/management/DaemonTokenCreateModal.tsx` returns `1`
    - `grep -c "data-token-dismiss" apps/web/src/components/management/DaemonTokenCreateModal.tsx` returns `1`
    - `grep -c "data-token-copy-button" apps/web/src/components/management/DaemonTokenCreateModal.tsx` returns `1`
    - `grep -c "useState<string | null>" apps/web/src/components/management/DaemonTokenCreateModal.tsx` returns `>=1`
    - `grep -c "setPlaintext" apps/web/src/components/management/DaemonTokenCreateModal.tsx` returns `>=2` (set + clear)
    - `grep -c "navigator.clipboard.writeText" apps/web/src/components/management/DaemonTokenCreateModal.tsx` returns `>=1`
    - HARD: `grep -rnE "localStorage|sessionStorage" apps/web/src/components/management/DaemonTokenCreateModal.tsx` returns 0
    - HARD: `grep -n "document.title" apps/web/src/components/management/DaemonTokenCreateModal.tsx` returns 0
    - HARD: `grep -rn "dangerouslySetInnerHTML" apps/web/src/components/management/` returns 0
    - HARD: `grep -rnE "\\{\\{plaintext" apps/web/src/i18n/locales/` returns 0
    - **HARD (Warning-4 revision fix): `grep -n "console\\." apps/web/src/components/management/DaemonTokenCreateModal.tsx` returns 0 matches** — plaintext MUST NOT reach the console via any `console.*` call (not just `console.log`). Prevents e.g. `console.error(new Error(plaintext))` or `console.debug({ token: plaintext })` regressions from ever landing.
    - Playwright `-g "token create form|token copy once"` exits 0 (2 passed)
  </acceptance_criteria>
  <done>
    Two-step create modal ships with plaintext confined to local useState. HARD invariants all grep-green: zero browser-storage, zero innerHTML, zero document.title mutation, **zero `console.*` calls anywhere in the file (Warning-4 revision fix)**, zero plaintext in a11y strings. Playwright proves plaintext disappears from DOM after dismiss and never appears in storage.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RevokeConfirmDialog + token revoke scenario</name>
  <files>
    apps/web/src/components/management/RevokeConfirmDialog.tsx
    apps/web/src/pages/DaemonTokensPage.tsx
    tests/e2e/management-uis.spec.ts
  </files>
  <read_first>
    - .planning/phases/25-management-uis/25-UI-SPEC.md lines 500-504 (Revoke confirmation i18n)
    - .planning/phases/25-management-uis/25-UI-SPEC.md lines 506-520 (Destructive confirmation pattern)
    - .planning/phases/25-management-uis/25-UI-SPEC.md lines 578-579 (RevokeConfirmDialog component spec)
    - apps/web/src/components/management/useDaemonTokens.ts from Task 1 (revoke mutation contract)
    - apps/web/src/components/management/ArchiveConfirmDialog.tsx from Plan 25-01 (pattern to mirror)
  </read_first>
  <behavior>
    - Test: Open revoke dropdown on an active token row → click Revoke → RevokeConfirmDialog opens with title interpolating token name and destructive-variant confirm button
    - Test: Click Confirm → DELETE /api/daemon-tokens/:id called → dialog closes → row status flips to `revoked` badge
    - Test: Press Escape → dialog closes without calling DELETE
    - Test: Cancel button has autoFocus (destructive confirm is not default per UI-SPEC keyboard contract)
  </behavior>
  <action>
    **1. RevokeConfirmDialog.tsx** — Named export `{ token: DaemonToken | null; onConfirm: () => Promise<void>; onOpenChange: (open: boolean) => void }`. Pattern mirrors `ArchiveConfirmDialog` from Plan 25-01.

    Layout: shadcn `<Dialog open={token !== null} onOpenChange={onOpenChange}>`:
    - `<DialogContent className="max-w-[480px]">`
    - `<DialogHeader><DialogTitle>{t('management.daemonTokens.revokeConfirm.title', { name: token?.name ?? '' })}</DialogTitle></DialogHeader>`
    - `<DialogDescription>{t('management.daemonTokens.revokeConfirm.body')}</DialogDescription>`
    - `<DialogFooter className="gap-3">`:
      - `<Button variant="outline" autoFocus onClick={() => onOpenChange(false)}>{t('common.buttons.cancel')}</Button>`
      - `<Button variant="destructive" data-token-revoke-confirm disabled={isSubmitting}>{t('...revokeConfirm.confirm')}</Button>`
    - Confirm handler:
      ```ts
      setSubmitting(true);
      try {
        await onConfirm();
        toast.success(t('management.daemonTokens.revoke.success'));
        onOpenChange(false);
      } catch {
        toast.error(t('management.daemonTokens.revoke.failed'));
      } finally { setSubmitting(false); }
      ```
    - Early return null when token is null.

    **2. DaemonTokensPage.tsx** — render `<RevokeConfirmDialog token={revokeTarget} onOpenChange={(o) => !o && setRevokeTarget(null)} onConfirm={async () => { if (!revokeTarget) return; await revoke(revokeTarget.id); setAnnouncement(t('management.daemonTokens.a11y.revoked', { name: revokeTarget.name })); }}>`. Confirm that `setAnnouncement` interpolates only `name` (never plaintext).

    **3. tests/e2e/management-uis.spec.ts** — un-skip `'token revoke'`:
    - Seed 1 active token by POST /api/daemon-tokens with name `E2E-Revoke-${Date.now()}` (capture the returned token.id; discard the plaintext — test doesn't need it for this scenario)
    - Navigate `/daemon-tokens`
    - Click action dropdown for the row `[data-token-row={id}]`, click `[data-token-revoke-open={id}]`
    - Assert dialog title contains the token's name
    - Click `[data-token-revoke-confirm]`
    - Assert dialog closes; assert the row's `[data-token-status]` attribute is now `revoked` (after refetch completes)
    - DB read: assert `revoked_at` column is non-null for this token id
  </action>
  <verify>
    <automated>npm run build -w @aquarium/shared &amp;&amp; npm run typecheck -w @aquaclawai/aquarium &amp;&amp; npm run build:ce -w @aquarium/web &amp;&amp; npm run lint -w @aquarium/web &amp;&amp; node apps/web/scripts/check-i18n-parity.mjs &amp;&amp; bash -c 'if grep -rn "dangerouslySetInnerHTML" apps/web/src/components/management/ 2&gt;/dev/null; then exit 1; fi; if grep -rnE "localStorage|sessionStorage" apps/web/src/components/management/ 2&gt;/dev/null; then exit 1; fi; exit 0' &amp;&amp; ! grep -n "console\\." apps/web/src/components/management/DaemonTokenCreateModal.tsx &amp;&amp; npx playwright test tests/e2e/management-uis.spec.ts -g "token create form|token copy once|token revoke"</automated>
  </verify>
  <acceptance_criteria>
    - 1 new file + 1 modified + 1 spec un-skipped
    - `grep -c "data-token-revoke-confirm" apps/web/src/components/management/RevokeConfirmDialog.tsx` returns `1`
    - `grep -c "variant=\"destructive\"" apps/web/src/components/management/RevokeConfirmDialog.tsx` returns `>=1`
    - `grep -c "autoFocus" apps/web/src/components/management/RevokeConfirmDialog.tsx` returns `>=1` (on Cancel button per UI-SPEC)
    - HARD invariants still green (innerHTML + storage + plaintext a11y + console.* all 0)
    - `grep -c "test.skip(" tests/e2e/management-uis.spec.ts` returns `0` (all 9 scenarios un-skipped OR Wave 4-only placeholders)
    - Playwright `-g "token create form|token copy once|token revoke"` exits 0 (3 passed)
  </acceptance_criteria>
  <done>
    Full MGMT-03 surface live: create (with copy-once plaintext flow), list (with derived statuses), revoke (with destructive confirmation). HARD invariants grep-green (including console.* absence from Warning-4 fix). All 3 Playwright scenarios passing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→server | Cookie-JWT authenticated POST/GET/DELETE to `/api/daemon-tokens/*` through existing `requireAuth` middleware |
| server→browser | `DaemonTokenCreatedResponse` carries plaintext EXACTLY ONCE; `DaemonToken` projection NEVER contains plaintext or hash (type-enforced) |
| client-side plaintext scope | `useState<string \| null>(null)` inside `DaemonTokenCreateModal` is the ONLY container; cleared on dismiss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03-01 | Information Disclosure (HIGH — plaintext daemon token leak) | DaemonTokenCreateModal | mitigate | Plaintext lives only in `useState<string \| null>`; cleared on dismiss; CI grep guards enforce no `localStorage`/`sessionStorage`/`dangerouslySetInnerHTML` under `components/management/**`; PER-TASK grep checks enforce no `document.title` mutation AND no `console.*` calls of ANY kind in `DaemonTokenCreateModal.tsx` (Warning-4 fix); i18n `a11y.copied` = `"Token copied to clipboard"` with zero interpolation; `onCreated(token)` callback receives `DaemonToken` projection only (type-enforced, no plaintext field); Playwright scenario `token copy once` asserts plaintext absent from DOM + storage after dismiss |
| T-25-03-02 | Tampering (XSS via token name rendering) | DaemonTokenList + RevokeConfirmDialog | mitigate | Token name rendered as React text child (auto-escaped); no markdown pipeline for token names; CI grep guard blocks `dangerouslySetInnerHTML` under `components/management/**` |
| T-25-03-03 | Clickjacking (revoke confirmation) | RevokeConfirmDialog | mitigate | shadcn `Dialog` (`@radix-ui/react-dialog`) ships focus trap + `aria-modal` + overlay click-outside + Escape close; Cancel button `autoFocus` per UI-SPEC keyboard contract so destructive is not default action |
| T-25-03-04 | Tampering (CSRF on POST/DELETE) | useDaemonTokens + DaemonTokenCreateModal | mitigate | Reuses existing cookie + same-origin middleware (`requireAuth` from Phase 19); no new surface |
| T-25-03-05 | Information Disclosure (token hash enumeration via list endpoint) | DaemonTokenList | accept | Server projection (`DaemonToken`) has NO `tokenHash` field — type-enforced; client cannot render what server never sends |
| T-25-03-06 | Repudiation (user denies creating a token) | POST /api/daemon-tokens | accept | Server persists `created_by_user_id` from auth context (Phase 19-03); UI does not need additional audit trail in CE |
| T-25-03-07 | Information Disclosure (plaintext leaked via `aria-live` polite region) | DaemonTokensPage announcement | mitigate | `management.daemonTokens.a11y.copied` = `"Token copied to clipboard"` (zero interpolation); `a11y.created` interpolates `{{name}}` only; grep guard `rg "\\{\\{plaintext" apps/web/src/i18n/locales/` returns 0 matches |
| T-25-03-08 | Information Disclosure (plaintext visible in browser back/forward cache) | DaemonTokenCreateModal | mitigate | Plaintext only exists while modal is mounted; dismiss sets it to null and unmounts content; Dialog content lives in portal that React tears down on close |
| T-25-03-09 | Information Disclosure (plaintext leak via `console.*` output) | DaemonTokenCreateModal | mitigate | Per-task grep guard `! grep -n "console\\." apps/web/src/components/management/DaemonTokenCreateModal.tsx` enforces ZERO console calls in the file — not just `console.log` but ALL variants (warn/error/debug/info/trace). Warning-4 revision fix. Prevents regression where a developer adds `console.error(e)` in the submit catch block and leaks plaintext via error properties. |
</threat_model>

<verification>
Wave 3 green when:
1. `npm run build -w @aquarium/shared && npm run typecheck -w @aquaclawai/aquarium && npm run build:ce -w @aquarium/web && npm run lint -w @aquarium/web` all exit 0
2. `node apps/web/scripts/check-i18n-parity.mjs` exits 0
3. HARD invariants (MGMT-03):
   - `grep -rnE "localStorage|sessionStorage" apps/web/src/components/management/DaemonTokenCreateModal.tsx` → 0
   - `grep -rnE "localStorage|sessionStorage" apps/web/src/components/management/` → 0 (wider guard)
   - `grep -rn "dangerouslySetInnerHTML" apps/web/src/components/management/` → 0
   - `grep -n "document.title" apps/web/src/components/management/DaemonTokenCreateModal.tsx` → 0
   - **`grep -n "console\\." apps/web/src/components/management/DaemonTokenCreateModal.tsx` → 0 (Warning-4 revision fix)**
   - `grep -rnE "\\{\\{plaintext" apps/web/src/i18n/locales/` → 0
4. CI grep guards (from 25-00) exit 0
5. `npx playwright test tests/e2e/management-uis.spec.ts -g "token create form|token copy once|token revoke"` — 3 passed
6. Manual: token creation flow shows plaintext once; after dismiss, plaintext gone from DOM; page reload shows row with active status and NO plaintext anywhere in HTML
</verification>

<success_criteria>
- MGMT-03 SC MET: user can issue a new daemon token with friendly name + optional expiry, copy plaintext once, list existing tokens, and revoke with confirmation
- Plaintext shown EXACTLY ONCE and never persisted client-side after dismiss (proven by Playwright DOM + storage assertions)
- Derived status rendering correct for active / expiring_soon / expired / revoked
- Destructive confirmation pattern for revoke
- All strings pull from `management.daemonTokens.*`
- HARD grep invariants green (localStorage/sessionStorage/innerHTML/document.title/**console.*** (Warning-4)/plaintext-in-a11y all 0)
</success_criteria>

<output>
After completion, create `.planning/phases/25-management-uis/25-03-SUMMARY.md` documenting:
- All 6 new files under components/management/ + DaemonTokensPage.tsx rewrite
- 3 Playwright scenarios un-skipped and passing
- Data-attribute markers applied (data-token-row, data-token-status, data-token-plaintext, data-token-dismiss, data-token-copy-button, data-token-revoke-confirm, data-token-create-open, data-token-form-field, data-token-form-submit, data-token-revoke-open)
- MGMT-03 HARD invariant verification — all 6 grep guards green (including the new `console.*` guard from Warning-4 revision fix)
- Explicit call-out that plaintext never touched localStorage, sessionStorage, URL, document.title, **console (any level)**, or a11y announcer
- MGMT-03 requirement completion
</output>
