---
phase: 25-management-uis
plan: 01
type: execute
wave: 1
depends_on: [25-00]
files_modified:
  - apps/web/src/pages/AgentsPage.tsx
  - apps/web/src/components/management/AgentList.tsx
  - apps/web/src/components/management/AgentFormDialog.tsx
  - apps/web/src/components/management/CustomEnvEditor.tsx
  - apps/web/src/components/management/CustomArgsEditor.tsx
  - apps/web/src/components/management/ArchiveConfirmDialog.tsx
  - apps/web/src/components/management/useAgents.ts
  - apps/web/src/components/management/EmptyState.tsx
  - apps/web/src/components/management/time.ts
  - tests/e2e/management-uis.spec.ts
autonomous: true
requirements: [MGMT-01]

must_haves:
  truths:
    - "User can see a list of active agents with name, runtime badge, **status badge (Agent.status enum)**, max_concurrent_tasks, and updated time — SC-1 complete"
    - "User can click 'New agent' to open a form, fill name/instructions/runtime/custom_env/custom_args/maxConcurrentTasks, click Create, and see the new agent in the list"
    - "User can click Archive on an agent row, confirm in the dialog, and see the agent disappear from Active tab and appear in Archived tab"
    - "Active and Archived tabs both render correctly"
  artifacts:
    - path: "apps/web/src/pages/AgentsPage.tsx"
      provides: "Route component for /agents — orchestrates list, form dialog, archive dialog, tabs"
      contains: "AgentList"
    - path: "apps/web/src/components/management/AgentList.tsx"
      provides: "shadcn Table rendering of agents with row actions dropdown. Columns Name/Runtime/Status/MaxConcurrent/Updated/Actions — Status column renders Agent.status enum (idle/working/blocked/error/offline) as shadcn Badge per ROADMAP SC-1"
      contains: "data-agent-row"
    - path: "apps/web/src/components/management/AgentFormDialog.tsx"
      provides: "Create/edit form Dialog with all form fields per UI-SPEC"
      contains: "data-agent-form-submit"
    - path: "apps/web/src/components/management/CustomEnvEditor.tsx"
      provides: "Key-value row editor component"
      contains: "data-agent-env-add"
    - path: "apps/web/src/components/management/CustomArgsEditor.tsx"
      provides: "Tag-input editor component for custom args"
      contains: "data-agent-args-input"
    - path: "apps/web/src/components/management/ArchiveConfirmDialog.tsx"
      provides: "Destructive confirmation Dialog for archive + restore"
      contains: "data-agent-archive-confirm"
    - path: "apps/web/src/components/management/useAgents.ts"
      provides: "Data hook with fetch + create + update + archive + restore via api.ts wrapper"
    - path: "tests/e2e/management-uis.spec.ts"
      provides: "Un-skipped scenarios: 'agents list renders', 'agent form create', 'agent archive'"
      contains: "test('agents list renders'"
  key_links:
    - from: "apps/web/src/pages/AgentsPage.tsx"
      to: "/api/agents"
      via: "api.get<Agent[]>('/agents') + api.get<Agent[]>('/agents?includeArchived=true')"
      pattern: "api.get.*agents"
    - from: "apps/web/src/components/management/AgentFormDialog.tsx"
      to: "/api/agents (POST) and /api/agents/:id (PATCH)"
      via: "api.post / api.patch through useAgents.create / useAgents.update"
      pattern: "api\\.(post|patch)"
    - from: "apps/web/src/components/management/ArchiveConfirmDialog.tsx"
      to: "/api/agents/:id (DELETE) and /api/agents/:id/restore (POST)"
      via: "api.delete / api.post through useAgents.archive / useAgents.restore"
      pattern: "api\\.(delete|post)"
    - from: "apps/web/src/components/management/AgentList.tsx"
      to: "agent.status enum from packages/shared/src/v14-types.ts line 62"
      via: "Status column renders <Badge> with label from `management.agents.status.{idle|working|blocked|error|offline}`"
      pattern: "agent-status-badge|data-column=\"status\"|management\\.agents\\.status"
---

<objective>
MGMT-01: browse, create, edit, archive, and restore Agents. Ship the full Agents page with shadcn Table-based list, two-tab (Active / Archived) UX, create + edit form Dialog with all form fields (name, instructions, runtime selector, custom_env key-value editor, custom_args tag input, max_concurrent_tasks), archive + restore confirmation dialogs, and 3 Playwright scenarios matching VALIDATION.md titles verbatim.

**Revision note (Blocker 3 fix):** ROADMAP SC-1 explicitly requires the Agents page list to show `status` alongside `runtime` badge and `max_concurrent_tasks`. Confirmed from `packages/shared/src/v14-types.ts` line 62: `Agent.status = 'idle' | 'working' | 'blocked' | 'error' | 'offline'` — a 5-value enum that is MEANINGFULLY DISTINCT from the `archivedAt` soft-delete state. This plan now renders a dedicated Status column using a shadcn Badge whose label comes from `management.agents.status.{idle|working|blocked|error|offline}` (i18n keys pre-seeded in Plan 25-00 Task 1). The archived tab's "Archived" badge is supplemental (status of the agent lifecycle) and does NOT replace the Agent.status enum rendering.

Purpose: Deliver the primary agent-management surface that users reach from the sidebar. Every form field and action required by MGMT-01 is implemented in full — no "v1 simplified" shortcuts.

Output:
- Fully wired `/agents` page with tabs, table (Name / Runtime / **Status** / MaxConcurrent / Updated / Actions), form dialog, archive/restore dialog
- 7 new files under `apps/web/src/components/management/`
- 3 Playwright scenarios un-skipped and green: `agents list renders`, `agent form create`, `agent archive`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-management-uis/25-UI-SPEC.md
@.planning/phases/25-management-uis/25-VALIDATION.md
@.planning/phases/25-management-uis/25-00-SUMMARY.md
@.planning/phases/17-agent-issue-comment-services/17-01-SUMMARY.md
@apps/server/src/routes/agents.ts
@apps/web/src/api.ts
@apps/web/src/components/ui/dialog.tsx
@apps/web/src/components/ui/table.tsx
@apps/web/src/components/ui/tabs.tsx
@apps/web/src/components/ui/select.tsx
@apps/web/src/components/ui/dropdown-menu.tsx
@apps/web/src/components/ui/badge.tsx
@apps/web/src/pages/IssuesBoardPage.tsx
@packages/shared/src/v14-types.ts

<interfaces>
Server endpoints (shipped from Phase 17 — confirmed in apps/server/src/routes/agents.ts):
- `GET /api/agents[?includeArchived=true]` → `ApiResponse<Agent[]>`
- `GET /api/agents/:id` → `ApiResponse<Agent>`
- `POST /api/agents` with body `{ name, runtimeId?, instructions?, customEnv?, customArgs?, maxConcurrentTasks?, visibility?, avatarUrl?, description? }` → `ApiResponse<Agent>` (201) or 400 on validation error
- `PATCH /api/agents/:id` with `UpdateAgentPatch` body → `ApiResponse<Agent>`
- `DELETE /api/agents/:id` → `ApiResponse<Agent>` (soft archive; 404 if already archived)
- `POST /api/agents/:id/restore` → `ApiResponse<Agent>`

Shared types (from packages/shared/src/v14-types.ts — confirmed during revision):
```ts
// Line 62-63:
export type AgentStatus = 'idle' | 'working' | 'blocked' | 'error' | 'offline';
export type AgentVisibility = 'private' | 'workspace' | 'public';

// Line 65-84:
export interface Agent {
  id: string; workspaceId: string; runtimeId: string | null;
  name: string; avatarUrl: string | null; description: string | null;
  instructions: string; customEnv: Record<string, string>; customArgs: string[];
  maxConcurrentTasks: number; visibility: AgentVisibility;
  status: AgentStatus;                        // 5-value enum, see line 62
  ownerUserId: string | null; archivedAt: string | null; archivedBy: string | null;
  createdAt: string; updatedAt: string;
}
export interface Runtime {
  id: string; workspaceId: string; name: string;
  kind: RuntimeKind; provider: RuntimeProvider; status: RuntimeStatus;
  // ...
}
```

API wrapper (apps/web/src/api.ts — shipped; unwraps `ApiResponse<T>` and throws `ApiError`):
```ts
api.get<T>(url): Promise<T>
api.post<T>(url, body?): Promise<T>
api.patch<T>(url, body?): Promise<T>
api.delete<T>(url): Promise<T>
```

shadcn Dialog primitive (apps/web/src/components/ui/dialog.tsx):
- `<Dialog open={bool} onOpenChange={fn}>` + `<DialogContent>` + `<DialogHeader>` + `<DialogTitle>` + `<DialogDescription>` + `<DialogFooter>`
- Auto-portals to `--z-modal` (z-index 1000) per the Phase 23 ladder
- Focus trap + `aria-modal` + Escape-to-close + overlay-click-to-close built-in (mitigates clickjacking)

shadcn Badge primitive (apps/web/src/components/ui/badge.tsx):
- Variants: `default | secondary | destructive | outline` — used for Status column rendering (mapping below)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AgentList (with SC-1 Status column) + AgentsPage tabs + useAgents hook + 'agents list renders' Playwright scenario</name>
  <files>
    apps/web/src/pages/AgentsPage.tsx
    apps/web/src/components/management/AgentList.tsx
    apps/web/src/components/management/useAgents.ts
    apps/web/src/components/management/EmptyState.tsx
    apps/web/src/components/management/time.ts
    tests/e2e/management-uis.spec.ts
  </files>
  <read_first>
    - .planning/phases/25-management-uis/25-UI-SPEC.md lines 540-580 (Component Inventory for Agents page)
    - .planning/phases/25-management-uis/25-UI-SPEC.md lines 810-840 (Data-attribute markers for Agents)
    - .planning/phases/25-management-uis/25-UI-SPEC.md lines 870-880 (Agents interaction flow)
    - packages/shared/src/v14-types.ts lines 60-85 (AgentStatus enum + Agent shape — SC-1 Status column source)
    - apps/server/src/routes/agents.ts (FULL — endpoint shapes)
    - apps/web/src/api.ts (FULL — wrapper contract)
    - apps/web/src/components/ui/badge.tsx (FULL — variants available for Status column mapping)
    - apps/web/src/pages/IssuesBoardPage.tsx (pattern: mount, fetch, render; reuse shape)
    - apps/web/src/components/ui/table.tsx + tabs.tsx + dropdown-menu.tsx + skeleton.tsx (primitives API)
    - tests/e2e/issues-board.spec.ts (Playwright pattern for seeding via API + DB read + selector expectations)
  </read_first>
  <behavior>
    - Test: Mount /agents with zero agents → renders `data-page="agents"` + "New agent" button + empty state with `data-empty-agents`.
    - Test: Mount /agents with 3 seeded agents (via POST /api/agents) → 3 rows render with `data-agent-row={id}` and visible columns Name/Runtime/**Status**/MaxConcurrent/Updated. Tab "Active" is selected by default.
    - Test: Each row's Status cell renders a `<Badge>` marked with `data-agent-status-badge={agent.status}` whose visible text is the localized `management.agents.status.{status}` value.
    - Test: Switch to Archived tab when no archived agents exist → renders `data-agent-tab="archived"` active state + `management.agents.archivedEmpty` empty text.
  </behavior>
  <action>
    **1. `apps/web/src/components/management/time.ts`** — Export `formatRelativeTime(ts: string | null, locale: string): string` using `Intl.RelativeTimeFormat`. Returns `'Never'` / `'Just now'` / `'{n}m ago'` / `'{n}h ago'` / `'{n}d ago'` per locale. Helper consumed across all 3 management pages.

    **2. `apps/web/src/components/management/EmptyState.tsx`** — Named export `export function EmptyState({ icon: Icon, heading, body, cta, dataMarker }: { icon: LucideIcon; heading: string; body: string; cta?: { label: string; onClick: () => void }; dataMarker?: string })`. Renders shadcn `<Card>` with `p-8`, centered content, 48px icon in `text-muted-foreground`, h2 heading, body copy, optional primary-variant CTA button. Forwards `data-empty-${dataMarker}` on the outer element for Playwright.

    **3. `apps/web/src/components/management/useAgents.ts`** — Hook returning `{ active, archived, isLoading, error, refetch, create, update, archive, restore }`. Uses `api.get<Agent[]>('/agents')` + `api.get<Agent[]>('/agents?includeArchived=true')` on mount; derives `archived = includeArchived - active`. Mutation functions call `api.post<Agent>('/agents', body)` / `api.patch<Agent>('/agents/' + id, patch)` / `api.delete<Agent>('/agents/' + id)` / `api.post<Agent>('/agents/' + id + '/restore')`. All mutations call `refetch()` on success. Swallows `ApiError` into the `error` state (toast is caller's responsibility).

    **4. `apps/web/src/components/management/AgentList.tsx`** — Props `{ agents: Agent[]; runtimes: Runtime[]; isLoading: boolean; onEdit: (a: Agent) => void; onArchive: (a: Agent) => void; onRestore: (a: Agent) => void; archivedView: boolean; searchQuery: string }`.

    Renders shadcn `<Table>` with columns **Name / Runtime / Status / MaxConcurrent / Updated / Actions** (6 columns — Status column is new per ROADMAP SC-1 and Blocker-3 fix). Column headers use i18n: `management.agents.columns.{name|runtime|status|maxConcurrent|updated|actions}` (status column key `management.agents.columns.status` was pre-seeded by Plan 25-00 Task 1).

    Each row:
    - `<tr data-agent-row={agent.id}>` with Tailwind classes `hover:bg-[var(--color-primary)]/5 even:bg-muted/30 px-4 py-3 border-b border-border`
    - Name cell: `<td>` with `font-medium` + onClick → `onEdit(agent)`
    - Runtime cell: badge from `runtimes.find(r => r.id === agent.runtimeId)`; shows `t('management.agents.noRuntime')` if null. Uses `Server`/`Monitor`/`Globe` icon per `runtime.kind` (see UI-SPEC Color section).
    - **Status cell (NEW — ROADMAP SC-1 + Blocker-3 fix):** Render a `<Badge>` with `data-agent-status-badge={agent.status}` attribute. Label = `t(\`management.agents.status.\${agent.status}\`)` — always one of the 5 keys pre-seeded by Plan 25-00 Task 1 (`idle|working|blocked|error|offline`). Variant mapping (fixed, no conditionals beyond this switch):
      ```tsx
      const statusVariant: Record<AgentStatus, 'default' | 'secondary' | 'destructive' | 'outline'> = {
        idle:     'secondary',   // neutral grey
        working:  'default',     // primary — active work
        blocked:  'outline',     // subtle — needs attention but not destructive
        error:    'destructive', // red — surfaces problem
        offline:  'outline',     // muted — no activity
      };
      ```
      Cell structure: `<td><Badge variant={statusVariant[agent.status]} data-agent-status-badge={agent.status}>{t(\`management.agents.status.\${agent.status}\`)}</Badge></td>`. Import `AgentStatus` type from `@aquarium/shared` for the mapping record's key type.
    - MaxConcurrent cell: plain number
    - Updated cell: `formatRelativeTime(agent.updatedAt, locale)` + Tooltip with absolute time
    - Actions cell: shadcn `<DropdownMenu>` with `<MoreHorizontal>` trigger + items:
      - Edit (`data-agent-action="edit"`) → `onEdit(agent)`
      - Archive (`data-agent-action="archive"`) → `onArchive(agent)` — only in active view
      - Restore (`data-agent-action="restore"`) → `onRestore(agent)` — only in archived view

    Archived rows: add `opacity-70 text-muted-foreground` classes + an additional `<Badge variant="outline">{t('management.agents.archived')}</Badge>` rendered AFTER the name cell (this is the lifecycle-state indicator; the Status column continues to render `agent.status` for archived agents too — the two indicators are complementary, not redundant). Loading state: render 5 shadcn `<Skeleton>` rows. Empty + no-match states via `<EmptyState>` with `Bot` icon from lucide-react. Memoize each row with `React.memo` on `(agent.id, agent.updatedAt, agent.status)` (status change forces re-render).

    Column header for Status: `<th data-column="status">{t('management.agents.columns.status')}</th>` — `data-column` marker lets Playwright assert the column exists without relying on visible-text matching under any locale.

    **5. `apps/web/src/pages/AgentsPage.tsx`** — Replace the Wave 0 stub with the real page:
    - `const { t, i18n } = useTranslation()`, `const [searchParams, setSearchParams] = useSearchParams()` from react-router-dom, `const { active, archived, isLoading, error, refetch, create, update, archive, restore } = useAgents()`
    - `const [tab, setTab] = useState<'active' | 'archived'>(() => searchParams.get('tab') === 'archived' ? 'archived' : 'active')`
    - `const [searchQuery, setSearchQuery] = useState('')`
    - `const [formState, setFormState] = useState<{ open: boolean; mode: 'create' | 'edit'; agent: Agent | null }>({ open: false, mode: 'create', agent: null })` — form state lifted here; Task 2 wires AgentFormDialog
    - `const [archiveState, setArchiveState] = useState<{ open: boolean; agent: Agent | null; mode: 'archive' | 'restore' }>({ open: false, agent: null, mode: 'archive' })` — Task 3 wires
    - Layout: `<main data-page="agents" className="mx-auto max-w-[1200px] p-6 pb-8">` with h1 + description, toolbar with `<Input placeholder={t('management.agents.filter.search')} value={searchQuery} onChange=...>` + Primary `<Button>` "New agent" (data-agent-new-open) that calls `setFormState({ open: true, mode: 'create', agent: null })`, then shadcn `<Tabs>` with `data-agent-tab="active"` / `data-agent-tab="archived"` triggers, then filtered `<AgentList>` (filter client-side by `searchQuery` lowercase match on `agent.name`).
    - Also fetch runtimes via `api.get<Runtime[]>('/runtimes')` once on mount; pass to AgentList for runtime badge rendering.
    - Do NOT render AgentFormDialog or ArchiveConfirmDialog in this task — Tasks 2 + 3 add them. Wire placeholder `onEdit` / `onArchive` / `onRestore` callbacks that just set the state.

    **6. `tests/e2e/management-uis.spec.ts`** — un-skip the `'agents list renders'` scenario. The scenario:
    - Clears existing agents via API (DELETE loop like tests/e2e/issue-detail.spec.ts)
    - Seeds 3 agents via `POST /api/agents` with names `E2E-Agent-A`, `E2E-Agent-B`, `E2E-Agent-C`
    - Navigates to `http://localhost:5173/agents`
    - Asserts `[data-page="agents"]` visible, "New agent" button visible, 3 `[data-agent-row]` elements present with the 3 names
    - **SC-1 Status column assertion (Blocker-3):** Asserts at least one `[data-agent-status-badge]` element is visible per row; asserts the status badge's text matches `management.agents.status.<status>` for the seeded value (newly-created agents default to `idle` server-side per Phase 17 — confirm via reading one of the seeded agents' `agent.status` from the DB before running the visible-text assertion).
    - Switches to Archived tab: asserts `management.agents.archivedEmpty` text visible (since no archived agents)

    Keep scenarios 'agent form create' and 'agent archive' `.skip()`ed — Tasks 2 + 3 un-skip them.
  </action>
  <verify>
    <automated>npm run build -w @aquarium/shared &amp;&amp; npm run typecheck -w @aquaclawai/aquarium &amp;&amp; npm run build:ce -w @aquarium/web &amp;&amp; npm run lint -w @aquarium/web &amp;&amp; node apps/web/scripts/check-i18n-parity.mjs &amp;&amp; grep -q '"Status"\|data-column="status"\|agent-status-badge' apps/web/src/components/management/AgentList.tsx &amp;&amp; npx playwright test tests/e2e/management-uis.spec.ts -g "agents list renders"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 new files created at their specified paths
    - `grep -c "data-agent-row" apps/web/src/components/management/AgentList.tsx` returns `>=1`
    - `grep -c "data-page=\"agents\"" apps/web/src/pages/AgentsPage.tsx` returns `1`
    - `grep -c "data-agent-tab" apps/web/src/pages/AgentsPage.tsx` returns `>=1`
    - `grep -c "api\.get<Agent\[\]>" apps/web/src/components/management/useAgents.ts` returns `>=2` (active + archived fetches)
    - **ROADMAP SC-1 Status column (Blocker-3 fix):** `grep -q '"Status"\|data-column="status"\|agent-status-badge' apps/web/src/components/management/AgentList.tsx` returns a match (one of the 3 variants satisfies the column-exists assertion)
    - **ROADMAP SC-1 Status column:** `grep -q 'data-agent-status-badge' apps/web/src/components/management/AgentList.tsx` returns match (the status badge itself is rendered per-row)
    - **ROADMAP SC-1 Status column:** `grep -q 'management.agents.status' apps/web/src/components/management/AgentList.tsx` returns match (status enum keys consumed from i18n)
    - `grep -rn "dangerouslySetInnerHTML" apps/web/src/components/management/` returns 0 (CI guard passes)
    - Playwright `-g "agents list renders"` exits 0 — asserts 3 rows with visible status badges
    - Typecheck + build + lint exit 0
  </acceptance_criteria>
  <done>
    Navigating to /agents renders the page scaffold with working Active/Archived tabs, search toolbar, "New agent" button (no dialog yet), and agent table with 0-N rows. **AgentList has a visible Status column rendering Agent.status enum (5 values) via `<Badge data-agent-status-badge={status}>`** — satisfies ROADMAP SC-1 "status" requirement with real data, not a placeholder. Tab switch + filter work client-side. Playwright `agents list renders` passes including the Status column assertion.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AgentFormDialog + CustomEnvEditor + CustomArgsEditor + 'agent form create' Playwright scenario</name>
  <files>
    apps/web/src/components/management/AgentFormDialog.tsx
    apps/web/src/components/management/CustomEnvEditor.tsx
    apps/web/src/components/management/CustomArgsEditor.tsx
    apps/web/src/pages/AgentsPage.tsx
    tests/e2e/management-uis.spec.ts
  </files>
  <read_first>
    - .planning/phases/25-management-uis/25-UI-SPEC.md lines 270-330 (Agent form i18n keys — EXACT string labels for every form field)
    - .planning/phases/25-management-uis/25-UI-SPEC.md lines 72-90 (Agent form Dialog spacing — `max-w-[640px]`, `space-y-4`, etc.)
    - .planning/phases/25-management-uis/25-UI-SPEC.md lines 822-840 (Data-attribute markers: `data-agent-form-field`, `data-agent-env-add`, `data-agent-args-input`, etc.)
    - apps/server/src/routes/agents.ts (POST + PATCH contracts — confirm `maxConcurrentTasks` range 1-16 + `UNIQUE(workspace_id,name)` error mapping)
    - apps/web/src/components/ui/dialog.tsx + input.tsx + select.tsx + separator.tsx + scroll-area.tsx (primitives)
    - apps/web/src/components/management/AgentList.tsx (from Task 1 — confirm callbacks)
  </read_first>
  <behavior>
    - Test: Click "New agent" on /agents → AgentFormDialog opens with `data-agent-form-field="name"` focused.
    - Test: Fill name + instructions + select runtime + add 2 env rows + add 3 arg tags + set maxConcurrent=4 + click Create → POST /api/agents called with correct body; dialog closes; list refetches; toast "Agent saved"; new agent row appears with `data-agent-row={newId}`.
    - Test: Submit form with empty name → inline validation "Name is required"; POST NOT called.
    - Test: Submit form with duplicate name → server 400 → inline error "An agent with this name already exists in your workspace".
  </behavior>
  <action>
    **1. `apps/web/src/components/management/CustomEnvEditor.tsx`** — Named export with props `{ value: Record<string, string>; onChange: (v: Record<string, string>) => void }`. Internal state tracks an ordered array of `{ key, value, id }` rows (UUID-local ids). Controlled-uncontrolled hybrid: seeds from `value` once; `onChange` fires on every edit with `Object.fromEntries(rows.filter(r => r.key).map(r => [r.key, r.value]))` — duplicate keys: last write wins (matches i18n warning copy).

    Layout per row: `<div className="flex gap-2 items-center">` with 2 `<Input>` elements (`placeholder={t('management.agents.form.customEnv.keyPlaceholder')}` / `valuePlaceholder`) + remove `<Button variant="ghost" size="icon">` with `<X>` icon and `aria-label={t('management.agents.form.customEnv.removeRow')}`. Add `data-agent-env-row={index}` on outer div. Add row button `<Button variant="outline" size="sm" data-agent-env-add>` with `<Plus>` icon + `t('management.agents.form.customEnv.addRow')` label. Duplicate-key warning: `<p className="text-xs text-[var(--color-warning-subtle-text)]">` below the input group, interpolating `{{key}}` from `management.agents.form.customEnv.duplicateKey`.

    **2. `apps/web/src/components/management/CustomArgsEditor.tsx`** — Named export `{ value: string[]; onChange: (v: string[]) => void }`. Renders a flex-wrap of tag pills (one `<Badge>` per arg with `<X>` button `aria-label={t('management.agents.form.customArgs.remove', { arg })}`) plus a single `<Input data-agent-args-input>` whose `onKeyDown`: Enter → push non-empty trimmed value to `value`, clear input; Backspace on empty → pop last arg. `placeholder={t('management.agents.form.customArgs.placeholder')}`.

    **3. `apps/web/src/components/management/AgentFormDialog.tsx`** — Named export with props `{ mode: 'create' | 'edit'; agent?: Agent; runtimes: Runtime[]; open: boolean; onOpenChange: (open: boolean) => void; onSaved: (agent: Agent) => void }`.

    State shape (`AgentFormValue`):
    ```ts
    interface AgentFormValue {
      name: string;
      instructions: string;
      runtimeId: string | null;
      customEnv: Record<string, string>;
      customArgs: string[];
      maxConcurrentTasks: number; // default 1
    }
    ```
    Seed from `agent` when `mode='edit'`; reset to blank when `mode='create'`. Reset via `useEffect` on `[open, agent?.id]`.

    Layout: shadcn `<Dialog open={open} onOpenChange={onOpenChange}>` with `<DialogContent className="max-w-[640px]">` containing:
    - `<DialogHeader>` with title `management.agents.form.titleCreate|titleEdit`
    - `<ScrollArea>`-wrapped body with `space-y-4 p-6`:
      - Name field: `<Label>` + `<Input data-agent-form-field="name" autoFocus>`; hint `management.agents.form.name.hint`
      - Instructions field: `<Label>` + `<Textarea data-agent-form-field="instructions" className="min-h-[120px] leading-relaxed">`; hint + counter (only when `value.length > 3500`)
      - Runtime field: `<Label>` + shadcn `<Select data-agent-form-field="runtime" value={...} onValueChange={...}>` with items: `<SelectItem value="">` (= None option per UI-SPEC `runtime.none` copy) plus one item per runtime showing `{runtime.name} ({kind-label})`
      - `<Separator className="my-4">` + Custom env section header + `<CustomEnvEditor>`
      - `<Separator>` + Custom args section header + `<CustomArgsEditor>`
      - `<Separator>` + Max concurrent field: `<Input type="number" min={1} max={16} data-agent-form-field="maxConcurrent">` with inline validation error if < 1 or > 16.
    - `<DialogFooter className="mt-6 pt-4 border-t border-border gap-3">`:
      - `<Button variant="outline" data-agent-form-cancel onClick={() => onOpenChange(false)}>`
      - `<Button data-agent-form-submit disabled={isSubmitting}>` — text = create/save per mode

    Submit handler:
    - Local validation: empty name → set error via state, return. maxConcurrent out of range → set error, return.
    - Call `api.post<Agent>('/agents', body)` (create) or `api.patch<Agent>('/agents/' + agent.id, body)` (edit)
    - On success: call `onSaved(result)`, `toast.success(t('management.agents.form.saveSuccess'))`, sr-announce via `management.agents.a11y.saved`, `onOpenChange(false)`
    - On `ApiError` with status 400 matching `/UNIQUE constraint/` → set inline error `management.agents.form.nameCollision`
    - On other `ApiError` → `toast.error(t('management.agents.form.saveFailed'))`; dialog stays open for retry

    **4. Modify `apps/web/src/pages/AgentsPage.tsx`** (from Task 1) — render `<AgentFormDialog open={formState.open} mode={formState.mode} agent={formState.agent ?? undefined} runtimes={runtimes} onOpenChange={(o) => setFormState(s => ({ ...s, open: o }))} onSaved={(agent) => { refetch(); setFormState({ open: false, mode: 'create', agent: null }); }}>`. Wire the Active-tab "New agent" button + AgentList `onEdit` callback to open the dialog.

    **5. Un-skip `'agent form create'`** in tests/e2e/management-uis.spec.ts. Scenario:
    - Clear existing agents via API
    - Seed a runtime via API (or read existing)
    - Navigate `/agents`, click `[data-agent-new-open]`
    - Wait for dialog open, fill `[data-agent-form-field="name"]` with `E2E-Created-${Date.now()}`, fill `[data-agent-form-field="instructions"]` with `Created by Playwright`, select first runtime via `[data-agent-form-field="runtime"]`, click `[data-agent-env-add]`, fill key/value, type `--verbose` + Enter in `[data-agent-args-input]`, set maxConcurrent=4
    - Click `[data-agent-form-submit]`
    - Assert dialog closes, new row visible with `[data-agent-row=*]` matching the name, DB read confirms `customEnv`/`customArgs`/`maxConcurrentTasks` all persisted correctly
  </action>
  <verify>
    <automated>npm run build -w @aquarium/shared &amp;&amp; npm run typecheck -w @aquaclawai/aquarium &amp;&amp; npm run build:ce -w @aquarium/web &amp;&amp; npx playwright test tests/e2e/management-uis.spec.ts -g "agents list renders|agent form create"</automated>
  </verify>
  <acceptance_criteria>
    - 3 new files created, 1 modified
    - `grep -c "data-agent-form-submit" apps/web/src/components/management/AgentFormDialog.tsx` returns `1`
    - `grep -c "data-agent-env-add" apps/web/src/components/management/CustomEnvEditor.tsx` returns `1`
    - `grep -c "data-agent-args-input" apps/web/src/components/management/CustomArgsEditor.tsx` returns `1`
    - `grep -c "data-agent-form-field" apps/web/src/components/management/AgentFormDialog.tsx` returns `>=4` (name, instructions, runtime, maxConcurrent)
    - `grep -c "api.post<Agent>" apps/web/src/components/management/AgentFormDialog.tsx` returns `>=1`
    - `grep -rn "dangerouslySetInnerHTML" apps/web/src/components/management/` returns 0 (CI guard)
    - Playwright `-g "agents list renders|agent form create"` exits 0 (2 passed)
  </acceptance_criteria>
  <done>
    User can create a new agent end-to-end with all form fields. Duplicate-name collision surfaces the locale-correct error. Playwright flow green. No `dangerouslySetInnerHTML` anywhere under `components/management/`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ArchiveConfirmDialog + restore flow + 'agent archive' Playwright scenario</name>
  <files>
    apps/web/src/components/management/ArchiveConfirmDialog.tsx
    apps/web/src/pages/AgentsPage.tsx
    tests/e2e/management-uis.spec.ts
  </files>
  <read_first>
    - .planning/phases/25-management-uis/25-UI-SPEC.md lines 317-330 (archive/restore i18n copy)
    - .planning/phases/25-management-uis/25-UI-SPEC.md lines 506-520 (Destructive confirmation pattern — shadcn Dialog shape)
    - .planning/phases/25-management-uis/25-UI-SPEC.md lines 760-770 (Archive semantics invariant — soft-delete, historical FKs preserved)
    - apps/server/src/routes/agents.ts DELETE + POST /:id/restore handlers
    - apps/web/src/components/management/useAgents.ts (archive + restore mutation methods from Task 1)
  </read_first>
  <behavior>
    - Test: Click Archive on an agent row → ArchiveConfirmDialog opens with title interpolating agent name, destructive-variant confirm button.
    - Test: Click Confirm → DELETE /api/agents/:id called → dialog closes → row disappears from Active tab.
    - Test: Switch to Archived tab → row present with `[data-agent-row={id}]` and Archived badge.
    - Test: Click Restore → restore dialog opens (default variant, not destructive) → confirm → POST /api/agents/:id/restore → row disappears from Archived, reappears in Active.
  </behavior>
  <action>
    **1. `apps/web/src/components/management/ArchiveConfirmDialog.tsx`** — Named export with props `{ agent: Agent | null; mode: 'archive' | 'restore'; onConfirm: () => Promise<void>; onOpenChange: (open: boolean) => void }`.

    Layout: shadcn `<Dialog open={agent !== null} onOpenChange={onOpenChange}>`:
    - `<DialogContent className="max-w-[480px]">`
    - `<DialogHeader>` with title `management.agents.archiveConfirm.title` / `management.agents.restoreConfirm.title` interpolating `{{name: agent.name}}` (ONLY when agent is non-null — guard with early return null)
    - `<DialogDescription>` renders body copy
    - `<DialogFooter>` with Cancel (`variant="outline"`, autoFocus) + Confirm button:
      - Archive mode: `<Button variant="destructive" data-agent-archive-confirm disabled={isSubmitting}>` text = `management.agents.archiveConfirm.confirm` ("Archive agent")
      - Restore mode: `<Button data-agent-restore-confirm disabled={isSubmitting}>` (default variant — positive action) text = `management.agents.restoreConfirm.confirm`
    - Confirm click: `setSubmitting(true); try { await onConfirm(); toast.success(...); sr-announce via a11y key; onOpenChange(false); } catch (e) { toast.error(...); } finally { setSubmitting(false); }`
    - Focus trap: shadcn Dialog already traps; autoFocus on Cancel button is the default per UI-SPEC Keyboard contract.

    **2. Modify `apps/web/src/pages/AgentsPage.tsx`** — render `<ArchiveConfirmDialog agent={archiveState.agent} mode={archiveState.mode} onOpenChange={(o) => setArchiveState(s => ({ ...s, open: o, agent: o ? s.agent : null }))} onConfirm={async () => { if (!archiveState.agent) return; if (archiveState.mode === 'archive') await archive(archiveState.agent.id); else await restore(archiveState.agent.id); }}>`. Wire `onArchive` / `onRestore` callbacks from AgentList to set `archiveState`.

    Also render a sr-only live region once in the page tree: `<div role="status" aria-live="polite" className="sr-only">{announcement}</div>` where `announcement` is managed via local state and flipped on each mutation success to the `management.agents.a11y.saved|archived|restored` interpolated string.

    **3. Un-skip `'agent archive'`** in tests/e2e/management-uis.spec.ts. Scenario:
    - Seed 1 agent via POST /api/agents
    - Navigate /agents, locate `[data-agent-row]` for the agent, open its action dropdown, click `[data-agent-action="archive"]`
    - Wait for dialog, assert title contains the agent name
    - Click `[data-agent-archive-confirm]`
    - Assert dialog closes and the row is no longer visible in the Active tab
    - Click `[data-agent-tab="archived"]`
    - Assert the archived row is visible with the Archived badge
    - DB read confirms `archived_at` is non-null
  </action>
  <verify>
    <automated>npm run build -w @aquarium/shared &amp;&amp; npm run typecheck -w @aquaclawai/aquarium &amp;&amp; npm run build:ce -w @aquarium/web &amp;&amp; node apps/web/scripts/check-i18n-parity.mjs &amp;&amp; npx playwright test tests/e2e/management-uis.spec.ts -g "agents list renders|agent form create|agent archive"</automated>
  </verify>
  <acceptance_criteria>
    - 1 new file + 1 modified
    - `grep -c "data-agent-archive-confirm" apps/web/src/components/management/ArchiveConfirmDialog.tsx` returns `1`
    - `grep -c "data-agent-restore-confirm" apps/web/src/components/management/ArchiveConfirmDialog.tsx` returns `1`
    - `grep -c "variant=\"destructive\"" apps/web/src/components/management/ArchiveConfirmDialog.tsx` returns `>=1`
    - `grep -c "api.delete<Agent>\|api.post<Agent>.*/restore" apps/web/src/components/management/useAgents.ts` returns `>=2` (archive + restore endpoints reachable)
    - `grep -rn "dangerouslySetInnerHTML" apps/web/src/components/management/` returns 0 (CI guard)
    - `grep -c 'test.skip(' tests/e2e/management-uis.spec.ts` returns `<=6` (3 Wave 1 scenarios un-skipped; Waves 2-3 still skipped)
    - Playwright `-g "agents list renders|agent form create|agent archive"` exits 0 (3 passed)
  </acceptance_criteria>
  <done>
    Full MGMT-01 surface live: browse (list with SC-1 Status column), create (form), edit (form), archive (dialog + Archived tab), restore (dialog back to Active tab). Soft-delete semantics preserved (FKs intact — server handles). All 3 Playwright scenarios green. Destructive confirmation pattern verified (shadcn Dialog with destructive variant button, focus trap, Escape-to-close). No `dangerouslySetInnerHTML` under `components/management/**`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→server | Cookie-JWT authenticated `api.*` calls to `/api/agents/*` — covered by existing `requireAuth` middleware |
| server→browser | Agent objects rendered as React text children + runtime badges + status badges; `agent.instructions` stored as plain text in Textarea (no markdown preview in v1); `agent.status` rendered as Badge text child |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-01 | Tampering (XSS via `agent.instructions`) | AgentFormDialog + AgentList | mitigate | `agent.instructions` rendered ONLY in `<Textarea>` (auto-escaped by React) for form; AgentList does NOT display instructions — no markdown render surface introduced. CI grep guard (25-00) blocks `dangerouslySetInnerHTML` under `components/management/**`. |
| T-25-01-02 | Clickjacking (archive/restore confirmation) | ArchiveConfirmDialog | mitigate | Uses shadcn `<Dialog>` (`@radix-ui/react-dialog`) — ships focus trap + `aria-modal` + overlay click-outside + Escape close; overlay prevents underlying page clicks |
| T-25-01-03 | CSRF (create/update/archive write endpoints) | useAgents mutations | mitigate | Reuses existing cookie + same-origin `/api/*` routes; `requireAuth` middleware unchanged; no new CSRF surface introduced |
| T-25-01-04 | Spoofing (duplicate agent name collision) | POST /api/agents | mitigate | Server enforces UNIQUE(workspace_id, name) at DB layer; client maps `/UNIQUE constraint/` error to localized `nameCollision` copy — user cannot silently overwrite |
| T-25-01-05 | Information Disclosure (archive reveals owner user) | AgentList archived rows | accept | `ownerUserId` already in Agent projection from Phase 17; CE is single-user — no cross-tenant leak risk |
| T-25-01-06 | Denial of Service (unbounded customEnv/customArgs) | CustomEnvEditor + CustomArgsEditor | accept | Server soft-validates ranges per AGENT-02; clients can submit hundreds of rows but server persists as JSON; no amplification |
| T-25-01-07 | Tampering (XSS via `agent.status` enum) | AgentList Status column | accept | `agent.status` is a server-sourced enum — only 5 values accepted per `AgentStatus` type; no user-controlled string reaches the Badge label (label comes from i18n lookup `management.agents.status.{status}`); even if server returned a new enum value, i18next returns the key as fallback (text child, still auto-escaped by React) |
</threat_model>

<verification>
Wave 1 green when:
1. `npm run build -w @aquarium/shared` exits 0
2. `npm run typecheck -w @aquaclawai/aquarium` exits 0
3. `npm run build:ce -w @aquarium/web` exits 0
4. `npm run lint -w @aquarium/web` exits 0 errors
5. `node apps/web/scripts/check-i18n-parity.mjs` exits 0
6. CI grep guards still pass — `grep -rn "dangerouslySetInnerHTML" apps/web/src/components/management/` = 0; `grep -rnE "localStorage|sessionStorage" apps/web/src/components/management/` = 0
7. Playwright `npx playwright test tests/e2e/management-uis.spec.ts -g "agents list renders|agent form create|agent archive"` — 3 passed
8. **ROADMAP SC-1 Status column (Blocker-3):** `grep -q '"Status"\|data-column="status"\|agent-status-badge' apps/web/src/components/management/AgentList.tsx` matches
9. Visual spot-check: /agents renders with tab UI, working form Dialog with all fields from UI-SPEC, archived tab works, Status column shows colored badges per enum value
</verification>

<success_criteria>
- **MGMT-01 SC-1 MET: Agents page lists agents with runtime badge + STATUS (Agent.status enum rendered as shadcn Badge in dedicated column) + max_concurrent_tasks** — Blocker-3 fix resolves the missing column from the prior revision
- MGMT-01 SC-1 EDIT FORM MET: form includes name, instructions, runtime selector, customEnv, customArgs, maxConcurrentTasks
- Archive / Restore flows both functional via confirmation dialogs
- Active/Archived tab UX per UI-SPEC
- Status column uses i18n labels pre-seeded in Plan 25-00 Task 1 (`management.agents.status.{idle|working|blocked|error|offline}`)
- No scope reduction — every form field and action from UI-SPEC delivered (not "v1 simplified")
</success_criteria>

<output>
After completion, create `.planning/phases/25-management-uis/25-01-SUMMARY.md` documenting:
- All 7 new files under `components/management/` + modifications to AgentsPage.tsx
- 3 Playwright scenarios un-skipped and passing
- Data-attribute markers applied (list of every `data-agent-*` marker created, including `data-agent-status-badge` per Blocker-3 fix)
- Status column details: visible column with `data-column="status"`, per-row `<Badge data-agent-status-badge={agent.status}>`, variant mapping (`idle|working|blocked|error|offline` → `secondary|default|outline|destructive|outline`)
- i18n key usage (confirmation that every form label/placeholder/hint pulls from `management.agents.*`, and status labels from `management.agents.status.*`)
- MGMT-01 requirements completion + ROADMAP SC-1 "status" requirement explicitly closed
</output>
